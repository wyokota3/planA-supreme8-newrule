# 監査レポート: F-015
日時: 2026-07-23 10:49  
対象: F-015 (situations_v1 対応（ローダ・明示拒否・評価）)

## サマリ

- 受け入れ条件: 全7件中、実装済7件、未実装0件
- オーバー実装: なし
- リスク: 低
- テスト実行: read-only 規約により未実行。テストコードと既存成果物を静的確認
- 総合判定: すべての受け入れ条件に対応する実装を引用可能。重大な仕様・図・実装間の矛盾はない

## 受け入れ条件の実現状況

### F-015-1: ローダ

manifest優先、dir walk fallbackで決定的に列挙し、`contract_violation`を保持する。

**実現箇所**: `reports/situations_v1-eval-20260722/situations_common.py:84-92`

```python
recs = []
for suite in suites:
    mpath = os.path.join(data_root, suite, "manifest.jsonl")
    if os.path.exists(mpath):
        recs.extend(_enum_from_manifest(mpath, data_root, suite, split))
    else:  # フォールバック: dir walk
        recs.extend(_enum_from_walk(data_root, suite, split))
recs.sort(key=lambda r: (r["suite"], r["split"], r["sid"]))
return recs
```

**実現箇所**: `reports/situations_v1-eval-20260722/situations_common.py:110-117`

```python
out.append({
    "sid": sid,
    "suite": m.get("suite", suite),
    "split": sp,
    "motif": m.get("motif"),
    "contract_violation": bool(m.get("contract_violation", False)),
    "dir": os.path.join(data_root, suite, sp, sid),
})
```

dir walk時も`scenario.yaml`から違反フラグを復元する。

**実現箇所**: `reports/situations_v1-eval-20260722/situations_common.py:128-142`

```python
for d in sorted(glob.glob(os.path.join(base, "*"))):
    if not os.path.isdir(d):
        continue
    sid = os.path.basename(d)
    cv, motif = False, None
    ypath = os.path.join(d, "scenario.yaml")
    if yaml is not None and os.path.exists(ypath):
        with open(ypath, encoding="utf-8") as f:
            y = yaml.safe_load(f) or {}
        motif = y.get("motif")
        cv = bool((y.get("corruption") or {}).get("contract_violation", False))
```

GTは8層だけへ写し、`hazard` / `dynamics`を取り込まない。

**実現箇所**: `reports/situations_v1-eval-20260722/situations_common.py:175-185`

```python
t0, t1, t2, t3 = (fr.get(k) or {} for k in ("t0", "t1", "t2", "t3"))
return {
    "risk_tier": t0.get("risk_tier"),
    "t1_state": t1.get("state"),
    "t2_mode": t2.get("mode"),
    "t2_role": t2.get("role"),
    "t2_relation": t2.get("relation"),
    "t3_hypothesis": t3.get("hypothesis"),
    "quality_regime": t3.get("quality_regime"),
    "scene_regime": t3.get("scene_regime"),
}
```

**判定**: 実装済

**コメント**: 外部データをread-onlyで確認した結果、全6 suiteでtrain 80／eval 40、必須3ファイルの欠落0件、違反はcrp train 13／eval 5だった。`worlds/`は読んでいない。

### F-015-2: preflight 明示拒否・engine/fit実行前

version、ts、tracks型、フレーム数を固定順で検査し、構造化verdictを返す。

**実現箇所**: `reports/situations_v1-eval-20260722/situations_common.py:215-230`

```python
# 1. version 接頭辞(書き換えない)。
for i, fr in enumerate(pso_frames):
    v = fr.get("version")
    if not (isinstance(v, str) and v.startswith(_SNAPSHOT_PREFIX)):
        return _rej("bad_version", f"frame {i} version={v!r}")

# 2. ts 単調非減少。
prev = None
for i, fr in enumerate(pso_frames):
    ts = fr.get("ts")
    try:
        tsf = float(ts)
    except (TypeError, ValueError):
        return _rej("other", f"frame {i} ts が数値でない: {ts!r}")
    if prev is not None and tsf < prev:
        return _rej("ts_regression", f"frame {i} ts {tsf} < 前 {prev}")
```

**実現箇所**: `reports/situations_v1-eval-20260722/situations_common.py:233-255`

```python
for i, fr in enumerate(pso_frames):
    tr = fr.get("tracks")
    if tr is None:
        continue
    if not isinstance(tr, dict):
        return _rej("type_break", f"frame {i} tracks が dict でない: {type(tr).__name__}")
    for key in ("audio", "humans", "objects"):
        if key not in tr:
            continue
        lst = tr[key]
        if not isinstance(lst, list):
            return _rej("type_break",
                        f"frame {i} tracks.{key} が list でない: {type(lst).__name__}")
        for j, el in enumerate(lst):
            if not isinstance(el, dict):
                return _rej("type_break",
                            f"frame {i} tracks.{key}[{j}] が dict でない: {type(el).__name__}")

if gt_frame_count is not None and len(pso_frames) != gt_frame_count:
    return _rej("frame_count_mismatch",
                f"PSO {len(pso_frames)} 行 != GT {gt_frame_count} フレーム")
```

非違反trainの拒否は`false_reject` incidentとして除外される。

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:88-106`

```python
for r in recs_train_nonviol:
    sid = r["sid"]
    pso = pso_cache[sid]
    gtf = gt_cache[sid]
    pf = sc.preflight_validate(pso, len(gtf))
    if not pf["ok"]:
        incidents.append({
            "sid": sid,
            "suite": r.get("suite"),
            "split": r.get("split", "train"),
            "kind": "false_reject",
            "reason": pf["reason"],
            "detail": pf["detail"],
        })
        continue
    snaps[sid] = sc.prepare_snaps(pso)
    gt[sid] = [sc.gt_view(fr) for fr in gtf]
```

`main()`ではpreflight完了後にのみfitへ進む。

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:682-685,719-725`

```python
train_all_snaps, train_all_gt, train_incidents = build_train_inputs(
    train_nonviol, pso_cache, gt_cache
)
...
def base_all():
    if "base_all" not in shared:
        ts = time.perf_counter()
        shared["base_all"] = fit_supreme_strict_off(train_all_snaps, train_all_gt)
```

**判定**: 実装済

**コメント**: versionは参照だけで書き換えていない。拒否されたsidは学習用`snaps` / `gt`へ追加されない。

### F-015-3: 違反除外＋rejection_acc

違反・非違反を分離し、違反はeval実走対象から除外する。

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:661-676`

```python
train_recs = sc.enumerate_scenarios(data_root, split="train")
eval_recs = sc.enumerate_scenarios(data_root, split="eval")
train_viol = [r for r in train_recs if r["contract_violation"]]
train_nonviol = [r for r in train_recs if not r["contract_violation"]]
eval_viol = [r for r in eval_recs if r["contract_violation"]]
eval_nonviol = [r for r in eval_recs if not r["contract_violation"]]
...
rej_eval = rejection_tally(eval_viol, pso_cache, gt_cache)
rej_train = rejection_tally(train_viol, pso_cache, gt_cache)
```

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:507-527`

```python
for r in recs_viol:
    sid = r["sid"]
    pf = sc.preflight_validate(pso_cache[sid], len(gt_cache[sid]))
    rj = not pf["ok"]
    rejected += 1 if rj else 0
...
total = len(recs_viol)
return {
    "total": total,
    "rejected": rejected,
    "rejection_acc": (rejected / total) if total else float("nan"),
    "by_reason": by_reason,
    "detail": detail,
}
```

**判定**: 実装済

**コメント**: `results.json`にはeval 5/5、train 13/13が別々に記録され、非違反eval 235本だけが8層採点されている。

### F-015-4: per-suite＋pooled採点

非違反evalを実走し、長さ不一致やengine例外をincident化する。

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:458-481`

```python
pf = sc.preflight_validate(pso, len(gtf))
if not pf["ok"]:
    incidents.append({"sid": sid, "kind": "unexpected_preflight_reject",
                      "reason": pf["reason"], "detail": pf["detail"]})
    continue
snaps = sc.prepare_snaps(pso)
try:
    views = core.run_supreme_scenarios({sid: snaps}, params, config=STRICT_OFF)[sid]
except Exception as e:
    tb = traceback.format_exc().strip().splitlines()
    incidents.append({"sid": sid, "kind": "engine_crash", "error": repr(e),
                      "traceback_tail": tb[-3:]})
    continue
if len(views) != len(gtf):
    incidents.append({
        "sid": sid,
        "kind": "view_gt_length_mismatch",
        "engine_view_count": len(views),
        "gt_frame_count": len(gtf),
        "detail": f"engine views {len(views)} != GT frames {len(gtf)}",
    })
    continue
trace[sid] = sc.assemble_trace_frames(views, gtf)
```

`harness.score`をpooledとsuite別の双方で呼び、8層と層macro平均を保存する。

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:485-501`

```python
def score_trace(trace):
    spec = harness.canonical_metric_spec()

    def _score(sub):
        res = harness.score(sub, spec)
        return {
            "overall": res.overall(),
            "layers": {ly: res.layer_score(ly) for ly in sc.LAYERS},
            "n_scenarios": len(sub),
            "n_frames": sum(len(v) for v in sub.values()),
        }

    pooled = _score(trace)
    per_suite = {suite: _score(sub)
                 for suite, sub in sorted(sc.partition_by_suite(trace).items())}
    return pooled, per_suite
```

**実現箇所**: `src/supreme/harness.py:120-134`

```python
def overall(self) -> float:
    scores = []
    for layer in self.layers:
        c = self._counts[layer]
        if c["nonnull"] == 0:
            continue
        scores.append(c["correct"] / c["nonnull"])
    if not scores:
        return math.nan
    return sum(scores) / len(scores)
```

**判定**: 実装済

**コメント**: `results.json`は各configについて235シナリオ、4,057フレーム、pooledおよび6 suiteの8層スコアを保持する。READMEにもper-suite × per-layer表が生成されている。

### F-015-5: strict OFF必須

学習用viewを公開APIのstrict OFF実走から作る。

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:119-127`

```python
views_by_sid = core.run_supreme_scenarios(
    train_snaps, params=None, config=STRICT_OFF
)
for sid, snaps in train_snaps.items():
    snaps = list(snaps)
    gt_views = list(train_gt.get(sid, []))
    views = views_by_sid[sid]
    t3_samples.append(core._t3_practice_from_scenario(snaps, views, gt_views))
    scene_samples.append(core._scene_practice_from_scenario(snaps, gt_views))
```

T2入力はstrict/configを受け取らないcore helperから構築する。

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:369-382`

```python
sids_all = sorted(train_snaps.keys())
stride = max(1, len(sids_all) // core._T2_FIT_MAX_SCENARIOS)
t2_scens = []
for sid in sids_all[::stride][:core._T2_FIT_MAX_SCENARIOS]:
    snaps_s = list(train_snaps[sid])
    gt_views = list(train_gt.get(sid, []))
    feats = core._neupsl_inputs_from_scenario(snaps_s)
    gts = [{"mode": (gt_views[i] or {}).get("t2_mode") if i < len(gt_views) else None,
            "role": (gt_views[i] or {}).get("t2_role") if i < len(gt_views) else None,
            "rel": (gt_views[i] or {}).get("t2_relation") if i < len(gt_views) else None}
           for i in range(len(feats))]
```

eval実走もstrict OFFである。

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:464-466`

```python
snaps = sc.prepare_snaps(pso)
try:
    views = core.run_supreme_scenarios({sid: snaps}, params, config=STRICT_OFF)[sid]
```

**判定**: 実装済

**コメント**: `STRICT_OFF`は同ファイル`:54`で`{"strict_gt_conformance": False}`に固定されている。Git履歴上、F-015の実装コミットは`src/supreme/*.py`を変更していない。

### F-015-6: 決定性

同一paramsで同じ入力を2回実走し、viewを完全一致比較する。

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:533-545`

```python
def determinism_check(params, recs_eval_nonviol, pso_cache):
    sub = [r for r in recs_eval_nonviol if r["suite"] == "std"][:5]

    def _run():
        out = {}
        for r in sub:
            snaps = sc.prepare_snaps(pso_cache[r["sid"]])
            out[r["sid"]] = core.run_supreme_scenarios(
                {r["sid"]: snaps}, params, config=STRICT_OFF)[r["sid"]]
        return out

    a, b = _run(), _run()
    return {"subset": [r["sid"] for r in sub], "identical": (a == b)}
```

**判定**: 実装済

**コメント**: 列挙は明示ソート、T2シナリオもsidソート、preflight・整形・採点は固定順である。対象経路に乱数APIはない。`time.perf_counter()`はtimings metadataにのみ使用される。既存結果は全4 configで`identical: true`だが、実走確認対象は各configのstd先頭5本である。

### F-015-7: cache・分割実行のprovenance

cache manifestは指定された識別情報を持ち、完全一致だけを再利用する。

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:180-209`

```python
def cache_manifest(key, engine_head, data_root, data_head, recipe_id=LEARNING_RECIPE_ID):
    return {
        "schema_version": CACHE_SCHEMA_VERSION,
        "key": key,
        "engine_repo_head": engine_head,
        "data_root": os.path.abspath(data_root),
        "data_repo_head": data_head,
        "learning_recipe_id": recipe_id,
    }

def _cache_blob_matches(blob, expected_manifest):
    return (
        isinstance(blob, dict)
        and "value" in blob
        and blob.get("manifest") == expected_manifest
    )
```

strict ON時代のt3sceneは再利用せず、旧T2は再生成内容一致時だけ移行する。

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:287-299,326-362`

```python
if os.path.exists(t3_path):
    ...
    if _cache_blob_matches(t3_blob, manifests[t3_key]):
        report["t3scene"] = "current_strict_off"
    else:
        os.remove(t3_path)
        report["t3scene"] = "discarded_strict_on_or_unverifiable"
...
if not _legacy_value_matches(input_blob, regenerated_t2scens):
    ...
    report.update({
        "t2": "legacy_input_mismatch_discarded",
        "removed": removed,
        "training_blocked": True,
    })
    return report
...
blob["manifest"] = manifests[key]
blob["migration"] = {
    "source": LEGACY_MIGRATION_SOURCE,
    "validated_t2scens_equal": True,
}
```

configごとのprovenance欠落・不一致を既定拒否し、`--force-mixed`時だけ保持する。

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:594-623`

```python
for name, config_result in sorted(results.get("configs", {}).items()):
    provenance = (
        config_result.get("provenance") if isinstance(config_result, dict) else None
    )
    missing_keys = sorted(
        RESULT_PROVENANCE_KEYS - set(provenance)
        if isinstance(provenance, dict)
        else RESULT_PROVENANCE_KEYS
    )
    if missing_keys or provenance != current_provenance:
        mismatches.append({
            "config": name,
            "provenance": provenance,
            "missing_keys": missing_keys,
        })

if mismatches and not force_mixed:
    ...
    raise RuntimeError(
        "results.json provenance mismatch for config(s): "
        f"{names}; rerun them or pass --force-mixed explicitly"
    )
```

**判定**: 実装済

**コメント**: 現行`results.json`は各configにengine HEAD、data HEAD、data rootを個別保持している。provenance欠落を現在値で補完する処理はない。

## テストカバレッジ

- 静的確認したテスト定義: 42関数、parameterize展開時44ケース
- read-only規約によりpytestは実行していない
- 外部データルートは存在するため、実行時には2件の統合テストもskip条件に該当しない

テストされている振る舞い:

- clean、bad version、Delta、ts後退・等値、tracks型、フレーム数不一致: `tests/test_F015_situations_adapter.py:77-181`
- version非改変、min_TTC_sだけの補完、入力非破壊: `:187-229`
- GTの8層変換、hazard/dynamics除外、t3配下のquality/scene: `:235-272`
- index突合、view/GT長不一致、suite分割: `:278-307`
- false rejectの学習除外: `:313-335`
- 学習用t3 mode系列のstrict OFF透過: `:338-371`
- 違反sidがengine/fit/採点へ到達しないこと: `:374-462`
- eval strict OFF、長さ不一致incident、engine crash traceback: `:465-535`
- pooled/per-suiteでの`harness.score`呼び出し: `:538-572`
- cache manifest不一致、旧t3scene破棄、旧T2一致移行・不一致停止: `:575-655`
- results provenance不一致・欠落・必須キー欠落の既定拒否: `:658-711`
- resultsとREADMEのpooled/rejection値一致: `:714-740`
- 実データ720本・違反18本、eval違反拒否: `:750-780`

テストされていない振る舞い:

- manifestが存在しない場合のdir walk fallbackを直接通す合成テスト
- dir walk fallbackで`scenario.yaml`から違反フラグを復元する境界
- pyyaml不在時にGT読込を停止する経路
- 複数違反を同時に含む入力での固定優先順位、および`reason="other"`の直接検証
- `tracks.objects`固有の非list／非dict要素境界
- `fit_supreme_strict_off`全体のT2配線とcore学習順の数値的同一性
- `determinism_check`自体の単体テスト、および235本全体の2回実走
- READMEのper-suite、timings、incident表示とresultsの完全一致

## アーキテクチャ上の位置づけ

- 実現したノード: `sitload`, `sitrun`, `sitreport`
- 接続先ノード: `core`, `harness`
- このノード群に対する充足度: 完全

**図の定義**: `specs/ARCHITECTURE.md:33-37`

```mermaid
subgraph situations["situations_v1 能力評価アダプタ（F-015）"]
    sitload["situations loader + preflight<br/>違反拒否/false reject 除外"]
    sitrun["situations campaign runner<br/>strict OFF 学習・評価"]
    sitreport["situations results/report<br/>per-suite/pooled/incidents"]
end
```

**図の接続**: `specs/ARCHITECTURE.md:63-66`

```mermaid
sitload --> sitrun
sitrun --> core
sitrun --> harness
sitrun --> sitreport
```

- `sitload → sitrun`: `run_supreme_situations.py:51`の`import situations_common as sc`および列挙・preflight呼び出しと一致
- `sitrun → core`: strict OFFの学習view生成・eval実走と一致
- `sitrun → harness`: `score_trace()`によるpooled/per-suite採点と一致
- `sitrun → sitreport`: `results.json`出力と`make_report.py`によるREADME生成に一致
- 上流（入力元）の未実装: なし。外部situations_v1は720本と必須ファイルを確認済み
- 下流（出力先）の未実装: なし。`core`、`harness`、`t3`、`scene`、`quality`は`specs/status.json:54,58-60,62`ですべて`done`
- 管理メタデータ: `specs/status.json:125`のF-015 files一覧に純ロジック、ランナー、`make_report.py`、テスト、仕様、図が登録済み
- 図とコードのズレ: なし

## オーバー実装の検出

仕様にない実装: なし。

N1/N2/N3/N3-std、T2 bilevel手配線、段階cache、旧cache移行、`--force-mixed`、決定的README生成はSPECまたはADR 0058に明記されているため、オーバー実装とは判定しない。

## 推奨アクション

- 優先度P2: manifestなしのdir walk fallbackと、fallback時の違反フラグ復元を合成テストで固定する
- 優先度P2: pyyaml不在、複数違反の固定優先順位、`other` verdictを異常系テストへ追加する
- 優先度P2: `fit_supreme_strict_off`のT2配線と`determinism_check`を直接テストする
- 優先度P3: READMEとresultsについてper-suite、timings、incidentまで整合検証を拡張する
