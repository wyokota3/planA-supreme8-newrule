# 監査レポート: F-015
日時: 2026-07-23 07:30
対象: F-015 (situations_v1 対応（ローダ・明示拒否・評価）)

## サマリ
- 受け入れ条件: 全6件中、実装済4件、部分実装2件、完全未実装0件
- オーバー実装: あり
- リスク: 高
- 主な未充足:
  - train 非違反シナリオが preflight を経ず `fit_supreme` に渡される
  - N2/N3 系の学習用 engine 実行が strict ON であり、「能力評価の engine 実走は必ず strict OFF」という条件を満たさない
- テスト実行: 監査環境の権限制限により、pytest 起動時に `typing_extensions.py` の読み取りが拒否されたため未完了。以下のカバレッジ判定はテストコードの静的確認による

## 受け入れ条件の実現状況

### F-015-1: ローダ
**実現箇所**: `reports/situations_v1-eval-20260722/situations_common.py:83-91`

```python
recs = []
for suite in suites:
    mpath = os.path.join(data_root, suite, "manifest.jsonl")
    if os.path.exists(mpath):
        recs.extend(_enum_from_manifest(mpath, data_root, suite, split))
    else:  # フォールバック: dir walk
        recs.extend(_enum_from_walk(data_root, suite, split))
recs.sort(key=lambda r: (r["suite"], r["split"], r["sid"]))
return recs
```

**実現箇所**: `reports/situations_v1-eval-20260722/situations_common.py:109-116`

```python
out.append({
    "sid": sid,
    "suite": m.get("suite", suite),
    "split": sp,
    "motif": m.get("motif"),
    "contract_violation": bool(m.get("contract_violation", False)),
    "dir": os.path.join(data_root, suite, sp, sid),
})
```

**実現箇所**: `reports/situations_v1-eval-20260722/situations_common.py:127-141`

```python
for d in sorted(glob.glob(os.path.join(base, "*"))):
    if not os.path.isdir(d):
        continue
    sid = os.path.basename(d)
    cv, motif = False, None
    ypath = os.path.join(d, "scenario.yaml")
    if yaml is not None and os.path.exists(ypath):
        with open(ypath, encoding="utf-8") as f:
            y = yaml.safe_load(f) or {}
        motif = y.get("motif")
        cv = bool((y.get("corruption") or {}).get("contract_violation", False))
    out.append({
        "sid": sid, "suite": suite, "split": sp, "motif": motif,
        "contract_violation": cv, "dir": d,
    })
```

**実現箇所**: `reports/situations_v1-eval-20260722/situations_common.py:174-184`

```python
t0, t1, t2, t3 = (fr.get(k) or {} for k in ("t0", "t1", "t2", "t3"))
return {
    "risk_tier": t0.get("risk_tier"),
    "t1_state": t1.get("state"),
    "t2_mode": t2.get("mode"),
    "t2_role": t2.get("role"),
    "t2_relation": t2.get("relation"),
    "t3_hypothesis": t3.get("hypothesis"),
    "quality_regime": t3.get("quality_regime"),
    "scene_regime": t3.get("scene_regime"),
}
```

**判定**: 実装済

**コメント**: manifest 優先、dir walk フォールバック、決定的ソート、違反フラグ取得、8層変換を確認した。`hazard` と `dynamics` は返却 view に含まれていない。既存成果物 `results.json:17-24` には train 480本、eval 240本、違反 train 13本・eval 5本の列挙結果が記録されている。

### F-015-2: preflight 明示拒否・engine 実行前
**実現箇所**: `reports/situations_v1-eval-20260722/situations_common.py:209-229`

```python
for i, fr in enumerate(pso_frames):
    if not isinstance(fr, dict):
        return _rej("other", f"frame {i} が dict でない: {type(fr).__name__}")

for i, fr in enumerate(pso_frames):
    v = fr.get("version")
    if not (isinstance(v, str) and v.startswith(_SNAPSHOT_PREFIX)):
        return _rej("bad_version", f"frame {i} version={v!r}")

prev = None
for i, fr in enumerate(pso_frames):
    ts = fr.get("ts")
    try:
        tsf = float(ts)
    except (TypeError, ValueError):
        return _rej("other", f"frame {i} ts が数値でない: {ts!r}")
    if prev is not None and tsf < prev:
        return _rej("ts_regression", f"frame {i} ts {tsf} < 前 {prev}")
```

**実現箇所**: `reports/situations_v1-eval-20260722/situations_common.py:232-260`

```python
for i, fr in enumerate(pso_frames):
    tr = fr.get("tracks")
    if tr is None:
        continue
    if not isinstance(tr, dict):
        return _rej("type_break", f"frame {i} tracks が dict でない: {type(tr).__name__}")
    for key in ("audio", "humans", "objects"):
        if key not in tr:
            continue
        lst = tr[key]
        if not isinstance(lst, list):
            return _rej("type_break",
                        f"frame {i} tracks.{key} が list でない: {type(lst).__name__}")
        for j, el in enumerate(lst):
            if not isinstance(el, dict):
                return _rej("type_break",
                            f"frame {i} tracks.{key}[{j}] が dict でない: {type(el).__name__}")

if gt_frame_count is not None and len(pso_frames) != gt_frame_count:
    return _rej("frame_count_mismatch",
                f"PSO {len(pso_frames)} 行 != GT {gt_frame_count} フレーム")

return {"ok": True, "reason": None, "detail": ""}
```

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:226-244`

```python
for r in recs_eval_nonviol:
    sid = r["sid"]
    pso = pso_cache[sid]
    gtf = gt_cache[sid]
    pf = sc.preflight_validate(pso, len(gtf))
    if not pf["ok"]:
        crashes.append({"sid": sid, "kind": "unexpected_preflight_reject",
                        "reason": pf["reason"], "detail": pf["detail"]})
        continue
    snaps = sc.prepare_snaps(pso)
    try:
        views = core.run_supreme_scenarios({sid: snaps}, params, config=STRICT_OFF)[sid]
```

**未実装箇所の根拠**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:73-77`

```python
def build_train_inputs(recs_train_nonviol, pso_cache, gt_cache):
    """core.fit_supreme 用の {sid: prepared_snaps} と {sid: [gt_view,...]} を作る。"""
    snaps = {r["sid"]: sc.prepare_snaps(pso_cache[r["sid"]]) for r in recs_train_nonviol}
    gt = {r["sid"]: [sc.gt_view(fr) for fr in gt_cache[r["sid"]]] for r in recs_train_nonviol}
    return snaps, gt
```

**未実装箇所の根拠**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:385-390`

```python
def base_all():
    if "base_all" not in shared:
        snaps, gt = build_train_inputs(train_nonviol, pso_cache, gt_cache)
        ts = time.perf_counter()
        shared["base_all"] = core.fit_supreme(snaps, gt)
```

**判定**: 部分的に実装

**コメント**: eval 非違反と、rejection 集計対象の違反18本には preflight がある。一方、train 非違反467本は `prepare_snaps` から直接 `core.fit_supreme` に入り、preflight がない。「各フレームを engine 実行前に契約検証する」という条件に対し、train 経路が未実装である。

### F-015-3: 違反除外＋rejection_acc
**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:361-367`

```python
train_recs = sc.enumerate_scenarios(data_root, split="train")
eval_recs = sc.enumerate_scenarios(data_root, split="eval")
train_viol = [r for r in train_recs if r["contract_violation"]]
train_nonviol = [r for r in train_recs if not r["contract_violation"]]
eval_viol = [r for r in eval_recs if r["contract_violation"]]
eval_nonviol = [r for r in eval_recs if not r["contract_violation"]]
std_train_nonviol = [r for r in train_nonviol if r["suite"] == "std"]
```

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:270-290`

```python
for r in recs_viol:
    sid = r["sid"]
    pf = sc.preflight_validate(pso_cache[sid], len(gt_cache[sid]))
    rj = not pf["ok"]
    rejected += 1 if rj else 0
    if rj:
        by_reason[pf["reason"]] = by_reason.get(pf["reason"], 0) + 1
    detail.append({"sid": sid, "rejected": rj,
                   "reason": pf["reason"], "detail": pf["detail"]})
total = len(recs_viol)
return {
    "total": total,
    "rejected": rejected,
    "rejection_acc": (rejected / total) if total else float("nan"),
    "by_reason": by_reason,
    "detail": detail,
}
```

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:374-380`

```python
rej_eval = rejection_tally(eval_viol, pso_cache, gt_cache)
rej_train = rejection_tally(train_viol, pso_cache, gt_cache)
print(f"[reject] eval acc={rej_eval['rejection_acc']:.3f} "
      f"({rej_eval['rejected']}/{rej_eval['total']}) "
      f"train acc={rej_train['rejection_acc']:.3f} "
      f"({rej_train['rejected']}/{rej_train['total']})", flush=True)
```

**判定**: 実装済

**コメント**: 8層採点には `eval_nonviol` だけが渡される。`results.json:26-34` で eval 5/5、`README.md:24-25` で eval 5/5とtrain 13/13が別掲されている。

### F-015-4: per-suite＋pooled 採点
**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:223-245`

```python
def run_eval(params, recs_eval_nonviol, pso_cache, gt_cache):
    trace, crashes = {}, []
    for r in recs_eval_nonviol:
        sid = r["sid"]
        pso = pso_cache[sid]
        gtf = gt_cache[sid]
        pf = sc.preflight_validate(pso, len(gtf))
        if not pf["ok"]:
            crashes.append({"sid": sid, "kind": "unexpected_preflight_reject",
                            "reason": pf["reason"], "detail": pf["detail"]})
            continue
        snaps = sc.prepare_snaps(pso)
        try:
            views = core.run_supreme_scenarios({sid: snaps}, params, config=STRICT_OFF)[sid]
        except Exception as e:
            tb = traceback.format_exc().strip().splitlines()
            crashes.append({"sid": sid, "kind": "engine_crash", "error": repr(e),
                            "traceback_tail": tb[-3:]})
            continue
        trace[sid] = sc.assemble_trace_frames(views, gtf)
    return trace, crashes
```

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:248-264`

```python
def score_trace(trace):
    spec = harness.canonical_metric_spec()

    def _score(sub):
        res = harness.score(sub, spec)
        return {
            "overall": res.overall(),
            "layers": {ly: res.layer_score(ly) for ly in sc.LAYERS},
            "n_scenarios": len(sub),
            "n_frames": sum(len(v) for v in sub.values()),
        }

    pooled = _score(trace)
    per_suite = {suite: _score(sub)
                 for suite, sub in sorted(sc.partition_by_suite(trace).items())}
    return pooled, per_suite
```

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:478-493`

```python
results["configs"][name] = {
    "description": descriptions[name],
    "fit": fit_info,
    "eval": {
        "n_scenarios": pooled["n_scenarios"],
        "n_frames": pooled["n_frames"],
        "pooled": pooled,
        "per_suite": per_suite,
        "seconds": eval_sec,
    },
    "rejection": {
        "eval": rej_eval,
        "train_informational": rej_train,
    },
    "crash_incidents": crashes,
    "n_crash_incidents": len(crashes),
```

**判定**: 実装済

**コメント**: `harness.score` を変更せず、pooled と suite 別に同じ canonical 8層を採点している。既存成果物では全configについて235シナリオ・4,057フレーム、6 suiteのper-layer/overallが記録され、crashは0件である。

### F-015-5: strict OFF 必須
**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:52-54`

```python
MEASUREMENT_DATE = "2026-07-22"
STRICT_OFF = {"strict_gt_conformance": False}
ALL_CONFIGS = ("N1", "N2", "N3", "N3-std")
```

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:236-244`

```python
snaps = sc.prepare_snaps(pso)
try:
    views = core.run_supreme_scenarios({sid: snaps}, params, config=STRICT_OFF)[sid]
except Exception as e:
    tb = traceback.format_exc().strip().splitlines()
    crashes.append({"sid": sid, "kind": "engine_crash", "error": repr(e),
                    "traceback_tail": tb[-3:]})
    continue
```

**未実装箇所の根拠**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:92-98`

```python
for sid, snaps in train_snaps.items():
    snaps = list(snaps)
    gt_views = list(train_gt.get(sid, []))
    views = core._run_one_scenario(snaps, None, None)  # config=None → strict(supreme2)経路
    t3_samples.append(core._t3_practice_from_scenario(snaps, views, gt_views))
```

**未実装箇所の根拠**: `src/supreme/core.py:1007-1022`

```python
def _strict_gt_conformance(config):
    ...
    if isinstance(config, dict):
        return bool(config.get("strict_gt_conformance", True))
    return True
```

**未実装箇所の根拠**: `src/supreme/core.py:1576-1582`

```python
for sid, snaps in practice_scenarios.items():
    snaps = list(snaps)
    gt_views = list(gt.get(sid, []))
    # core の既定経路で 8 層 view を得る(t2_mode argmax は core の実経路そのもの)。
    views = _run_one_scenario(snaps, None, None)
    t3_samples.append(_t3_practice_from_scenario(snaps, views, gt_views))
    scene_samples.append(_scene_practice_from_scenario(snaps, gt_views))
```

**判定**: 部分的に実装

**コメント**: 最終eval traceはすべて strict OFF で実行される。しかしN2の `core.fit_supreme` とN3/N3-stdの `fit_t3_scene_only` は、学習用view生成を `config=None`、すなわちstrict ONで実行する。SPECが入力engineとして `core.fit_supreme` を含め、「能力評価の engine 実走は必ず strict OFF」と定めているため、学習経路を含めた条件は未充足である。

### F-015-6: 決定性
**実現箇所**: `reports/situations_v1-eval-20260722/situations_common.py:83-91`

```python
recs = []
for suite in suites:
    ...
recs.sort(key=lambda r: (r["suite"], r["split"], r["sid"]))
return recs
```

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:296-308`

```python
def determinism_check(params, recs_eval_nonviol, pso_cache):
    sub = [r for r in recs_eval_nonviol if r["suite"] == "std"][:5]

    def _run():
        out = {}
        for r in sub:
            snaps = sc.prepare_snaps(pso_cache[r["sid"]])
            out[r["sid"]] = core.run_supreme_scenarios(
                {r["sid"]: snaps}, params, config=STRICT_OFF)[r["sid"]]
        return out

    a, b = _run(), _run()
    return {"subset": [r["sid"] for r in sub], "identical": (a == b)}
```

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:314-321`

```python
def sanitize(obj):
    if isinstance(obj, float):
        return None if math.isnan(obj) else obj
    if isinstance(obj, dict):
        return {k: sanitize(v) for k, v in obj.items()}
    if isinstance(obj, (list, tuple)):
        return [sanitize(v) for v in obj]
    return obj
```

**判定**: 実装済

**コメント**: 列挙、preflight、整形、採点には乱数がなく、時刻はtimings metadataにのみ使用される。各configについて同一paramsで5本を2回実行するチェックがあり、既存 `results.json:421-429` では一致している。ただし全235本・pooled採点結果・学習paramsの再現性を確認するテストはなく、検証範囲は不十分である。

## テストカバレッジ
- テスト定義: `tests/test_F015_situations_adapter.py` に26件。うち外部データを使う2件はデータルート不在時にskipされる
- テストされている振る舞い:
  - clean preflightと同一入力でのverdict一致: `tests/test_F015_situations_adapter.py:73-82`
  - bad_version、ts後退、type_break、フレーム数不一致: `tests/test_F015_situations_adapter.py:88-152`
  - ts同値許容、geom欠落の非違反扱い: `tests/test_F015_situations_adapter.py:115-121`, `161-165`
  - version非改変とgeom補完: `tests/test_F015_situations_adapter.py:171-202`
  - GTの8層変換、hazard/dynamics除外、t3配下のscene/quality: `tests/test_F015_situations_adapter.py:208-241`
  - index突合、suite分割: `tests/test_F015_situations_adapter.py:247-271`
  - 外部データが存在する場合のみ、720本の件数とeval違反拒否: `tests/test_F015_situations_adapter.py:281-309`
- テストされていない振る舞い:
  - manifest.jsonl分岐とdir walk分岐を合成データで個別に検証するテスト
  - 列挙の決定的順序、manifest欠落時の`scenario.yaml`違反フラグ復元
  - pyyaml不在時にGT値を捏造せず停止すること
  - 複数違反が同時にある場合の固定検出優先順位
  - `tracks.objects`の非list・非dict要素
  - train全フレームがfit前にpreflightを通ること。実装自体も存在しない
  - 契約違反シナリオでengineが一度も呼ばれないことをmockで確認するテスト
  - `rejection_tally`、eval/train別集計、8層分母除外
  - `run_eval`のengine例外、sid・トレースバック要約、採点分母除外
  - `score_trace`が実際に`harness.score`をsuite別・pooledで呼ぶこと
  - eval engineへのstrict OFF透過と、学習経路のstrict設定
  - 235本全体、pooledスコア、学習paramsを含むend-to-end決定性
  - cache有無および既存results.json有無で結果内容が変化しないこと
- 実行結果: 監査環境ではpytest依存ファイルへのアクセス拒否により実行不能。リポジトリ内文書の「全緑」記録は、今回の監査では再検証できていない

## アーキテクチャ上の位置づけ
- 実現したノード: `core`、`harness`を利用する外部評価アダプタ
- 関連する実行ノード: `epiin` → `strong` / `mode` / `relation` / `t3` / `scene` / `quality` → `epiout` → `harness`
- このノードに対する充足度: 部分
- 根拠:
  - `specs/status.json:118` はF-015を `nodes: ["core", "harness"]` に割り当てている
  - 実コードは `run_supreme_situations.py:238` で`core`を呼び、同ファイル`250-256`で`harness`を呼ぶ
  - ただしF-015が実装するloader/preflight/rejection分岐は`core`や`harness`そのものではなく、その上流に置かれたキャンペーンアダプタである
- 上流（入力元）の未実装:
  - train非違反入力から`core.fit_supreme`への経路にpreflightゲートがない
- 下流（出力先）の未実装:
  - per-suite/pooled採点、rejection、incident、timings、git HEADの出力は実装済
  - F-015に直接対応する下流処理の未実装は確認されなかった
- 図とコードのズレ: あり
  - `specs/ARCHITECTURE.md`にはF-015の記載が1件もない
  - システム図は`supreme --> harness`のみで、`situations_v1`入力、loader、preflight拒否分岐、rejection_acc、results/report出力を表現していない
  - `status.json:118`ではF-015を`core`/`harness`へ割り当てる一方、`status.json:54`のcoreノードと`:62`のharnessノードの`features`にはF-015が逆参照登録されていない
  - `ARCHITECTURE.md:143`のcore責務と`:141`のharness責務にもF-015アダプタ責務が反映されていない

## オーバー実装の検出
仕様にないが実装されているもの:

- `reports/situations_v1-eval-20260722/situations_common.py:277-282` で、仕様が認める`min_TTC_s=999.0`だけでなく、`overlap_path=False`と`lane_alignment=False`も補完している。既存geomで両キーだけが欠けた場合にも入力を変更する

```python
g = dict(s.get("geom") or {})
if g.get("min_TTC_s") is None:
    g["min_TTC_s"] = 999.0
g.setdefault("overlap_path", False)
g.setdefault("lane_alignment", False)
s["geom"] = g
```

- `reports/situations_v1-eval-20260722/situations_common.py:296-305` でengine view数とGT数が不一致でも、incident化せず短い側へ黙って切り詰める。仕様が定めるフレーム数検査はPSO行数対GT数であり、この追加挙動は採点分母を縮小し得る

```python
frames = []
n = min(len(views), len(gt_frames))
for i in range(n):
    gf = gt_frames[i]
    ts = gf.get("ts", float(i))
    frames.append({
        "ts": float(ts),
        "view": dict(views[i]),
        "gt": gt_view(gf),
    })
```

- `reports/situations_v1-eval-20260722/run_supreme_situations.py:121-140` に、SPECにないpickle永続キャッシュと再開機能がある。cache keyにデータHEAD、engine HEAD、レシピ値が含まれず、異なる入力・実装で作成した古いparamsを再利用できる

```python
path = os.path.join(cache_dir, key + ".pkl")
if os.path.exists(path):
    with open(path, "rb") as f:
        blob = pickle.load(f)
    return blob["value"], blob["compute_seconds"]
...
with open(path, "wb") as f:
    pickle.dump({"value": val, "compute_seconds": sec}, f)
```

- `reports/situations_v1-eval-20260722/run_supreme_situations.py:416-437`, `501-508` で既存`results.json`を読み、今回実行しないconfigの過去結果を保持する。データ/engine HEADが変わった後の分割実行では、新しいmeta配下に古いconfig結果が混在し得る

## 推奨アクション
- 優先度P0: `build_train_inputs`の前、または各fit呼び出し前に全train入力をpreflightし、拒否されたシナリオを学習へ渡さない
- 優先度P0: N2/N3/N3-stdの学習用view生成もstrict OFFに統一し、F-015-5を満たす。`src/supreme/*.py`を変更せず、アダプタ側でOFF対応の学習経路を構成する
- 優先度P1: geom補完を仕様どおり`min_TTC_s=999.0`だけに限定する
- 優先度P1: engine view数とGT数が不一致なら切り詰めず、構造化incidentとして記録して採点を中止する
- 優先度P1: cacheをデータHEAD・engine HEAD・学習レシピで検証し、不一致時は再利用しない
- 優先度P1: 既存results.jsonをマージする場合、configごとのデータHEAD・engine HEAD一致を必須にする
- 優先度P1: runner、rejection分母除外、strict透過、crash incident、全経路決定性を直接テストする
- 優先度P1: `ARCHITECTURE.md`にsituations_v1アダプタ、preflight拒否分岐、core/harnessへの接続、results出力を追加し、`status.json`の双方向ノード対応を一致させる
