# 監査レポート: F-015
日時: 2026-07-23 10:08
対象: F-015 (situations_v1 対応（ローダ・明示拒否・評価）)

## サマリ

- 受け入れ条件: 全7件中、実装済6件、部分的に実装1件、完全未実装0件
- 未充足: F-015-7 の results.json マージで、保存済み `data_root` が欠落した場合の拒否
- オーバー実装: なし
- リスク: 中
- テスト: provider の read-only 制約により未実行。以下は静的確認
- 総合判定: 実装本体はほぼ仕様を満たすが、provenance 検証の不足と重要経路のテスト欠落がある

## 受け入れ条件の実現状況

### F-015-1: ローダ

situations_v1 を manifest 優先、dir walk fallback で決定的に列挙し、違反フラグを分別する。ラベル形GTを8層へ変換し、`t2.hazard` / `t2.dynamics` は取り込まない。

**実現箇所**: `reports/situations_v1-eval-20260722/situations_common.py:84-92`

```python
recs = []
for suite in suites:
    mpath = os.path.join(data_root, suite, "manifest.jsonl")
    if os.path.exists(mpath):
        recs.extend(_enum_from_manifest(mpath, data_root, suite, split))
    else:  # フォールバック: dir walk
        recs.extend(_enum_from_walk(data_root, suite, split))
recs.sort(key=lambda r: (r["suite"], r["split"], r["sid"]))
return recs
```

**実現箇所**: `reports/situations_v1-eval-20260722/situations_common.py:110-117`

```python
out.append({
    "sid": sid,
    "suite": m.get("suite", suite),
    "split": sp,
    "motif": m.get("motif"),
    "contract_violation": bool(m.get("contract_violation", False)),
    "dir": os.path.join(data_root, suite, sp, sid),
})
```

dir walk 時の `scenario.yaml` からの違反フラグ復元は同ファイル `:133-141` に実装されている。

**実現箇所**: `reports/situations_v1-eval-20260722/situations_common.py:175-185`

```python
t0, t1, t2, t3 = (fr.get(k) or {} for k in ("t0", "t1", "t2", "t3"))
return {
    "risk_tier": t0.get("risk_tier"),
    "t1_state": t1.get("state"),
    "t2_mode": t2.get("mode"),
    "t2_role": t2.get("role"),
    "t2_relation": t2.get("relation"),
    "t3_hypothesis": t3.get("hypothesis"),
    "quality_regime": t3.get("quality_regime"),
    "scene_regime": t3.get("scene_regime"),
}
```

**判定**: 実装済

**コメント**: 返却viewは指定された8層だけであり、`hazard` / `dynamics` は含まれない。既存 `results.json:18-25` に train 480本、eval 240本、違反13本/5本の列挙実績が記録されている。

### F-015-2: preflight 明示拒否・engine/fit 実行前

全train/evalについて、version、ts後退、tracks型、PSO/GTフレーム数を固定順で検査し、構造化verdictを返す。非違反trainの拒否は `false_reject` incidentとして学習から除外する。

**実現箇所**: `reports/situations_v1-eval-20260722/situations_common.py:215-230`

```python
# 1. version 接頭辞(書き換えない)。
for i, fr in enumerate(pso_frames):
    v = fr.get("version")
    if not (isinstance(v, str) and v.startswith(_SNAPSHOT_PREFIX)):
        return _rej("bad_version", f"frame {i} version={v!r}")

# 2. ts 単調非減少。
prev = None
for i, fr in enumerate(pso_frames):
    ts = fr.get("ts")
    try:
        tsf = float(ts)
    except (TypeError, ValueError):
        return _rej("other", f"frame {i} ts が数値でない: {ts!r}")
    if prev is not None and tsf < prev:
        return _rej("ts_regression", f"frame {i} ts {tsf} < 前 {prev}")
```

**実現箇所**: `reports/situations_v1-eval-20260722/situations_common.py:233-255`

```python
for i, fr in enumerate(pso_frames):
    tr = fr.get("tracks")
    if tr is None:
        continue
    if not isinstance(tr, dict):
        return _rej("type_break", f"frame {i} tracks が dict でない: {type(tr).__name__}")
    for key in ("audio", "humans", "objects"):
        if key not in tr:
            continue
        lst = tr[key]
        if not isinstance(lst, list):
            return _rej("type_break",
                        f"frame {i} tracks.{key} が list でない: {type(lst).__name__}")
        for j, el in enumerate(lst):
            if not isinstance(el, dict):
                return _rej("type_break",
                            f"frame {i} tracks.{key}[{j}] が dict でない: {type(el).__name__}")

if gt_frame_count is not None and len(pso_frames) != gt_frame_count:
    return _rej("frame_count_mismatch",
                f"PSO {len(pso_frames)} 行 != GT {gt_frame_count} フレーム")
```

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:84-101`

```python
for r in recs_train_nonviol:
    sid = r["sid"]
    pso = pso_cache[sid]
    gtf = gt_cache[sid]
    pf = sc.preflight_validate(pso, len(gtf))
    if not pf["ok"]:
        incidents.append({
            "sid": sid,
            "suite": r.get("suite"),
            "split": r.get("split", "train"),
            "kind": "false_reject",
            "reason": pf["reason"],
            "detail": pf["detail"],
        })
        continue
    snaps[sid] = sc.prepare_snaps(pso)
    gt[sid] = [sc.gt_view(fr) for fr in gtf]
```

**判定**: 実装済

**コメント**: `main()` は `:674-680` で全非違反trainをpreflightした後、`:711`以降でfitを開始する。拒否されたsidは `snaps` / `gt` に追加されない。

### F-015-3: 違反除外＋rejection_acc

`contract_violation: true` を8層採点から除外し、eval 5本とtrain 13本を別々に rejection_acc 集計する。

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:653-668`

```python
train_recs = sc.enumerate_scenarios(data_root, split="train")
eval_recs = sc.enumerate_scenarios(data_root, split="eval")
train_viol = [r for r in train_recs if r["contract_violation"]]
train_nonviol = [r for r in train_recs if not r["contract_violation"]]
eval_viol = [r for r in eval_recs if r["contract_violation"]]
eval_nonviol = [r for r in eval_recs if not r["contract_violation"]]

pso_cache, gt_cache = load_caches(train_recs + eval_recs)

# rejection は config 非依存 → 一度だけ計算。
rej_eval = rejection_tally(eval_viol, pso_cache, gt_cache)
rej_train = rejection_tally(train_viol, pso_cache, gt_cache)
```

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:506-522`

```python
for r in recs_viol:
    sid = r["sid"]
    pf = sc.preflight_validate(pso_cache[sid], len(gt_cache[sid]))
    rj = not pf["ok"]
    rejected += 1 if rj else 0
    if rj:
        by_reason[pf["reason"]] = by_reason.get(pf["reason"], 0) + 1
    detail.append({"sid": sid, "rejected": rj,
                   "reason": pf["reason"], "detail": pf["detail"]})
total = len(recs_viol)
return {
    "total": total,
    "rejected": rejected,
    "rejection_acc": (rejected / total) if total else float("nan"),
    "by_reason": by_reason,
    "detail": detail,
}
```

**判定**: 実装済

**コメント**: engine実走には `eval_nonviol` だけが渡される（同ファイル `:788`）。既存結果はeval 5/5、train 13/13を別掲している（`results.json:29-162`）。

### F-015-4: per-suite＋pooled 採点

非違反evalのengine viewと生GTをindex対応で突合し、`harness.score`でsuite別・pooledの8層accと層macro平均を算出する。

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:461-476`

```python
views = core.run_supreme_scenarios({sid: snaps}, params, config=STRICT_OFF)[sid]
...
if len(views) != len(gtf):
    incidents.append({
        "sid": sid,
        "kind": "view_gt_length_mismatch",
        "engine_view_count": len(views),
        "gt_frame_count": len(gtf),
        "detail": f"engine views {len(views)} != GT frames {len(gtf)}",
    })
    continue
trace[sid] = sc.assemble_trace_frames(views, gtf)
```

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:480-496`

```python
def score_trace(trace):
    """trace を pooled + per-suite で 8 層採点する(harness.score・変更なし)。"""
    spec = harness.canonical_metric_spec()

    def _score(sub):
        res = harness.score(sub, spec)
        return {
            "overall": res.overall(),
            "layers": {ly: res.layer_score(ly) for ly in sc.LAYERS},
            "n_scenarios": len(sub),
            "n_frames": sum(len(v) for v in sub.values()),
        }

    pooled = _score(trace)
    per_suite = {suite: _score(sub)
                 for suite, sub in sorted(sc.partition_by_suite(trace).items())}
    return pooled, per_suite
```

`ScoreResult.overall()` の層macro平均は `src/supreme/harness.py:120-134` に実装されている。

**判定**: 実装済

**コメント**: engine例外は同ファイル `:462-465` でsid、例外、トレースバック末尾をincident化する。既存結果は非違反235本・4,057フレーム、6 suiteを報告している。

### F-015-5: strict OFF 必須

学習用view生成とeval実走をstrict OFFにし、T2入力はstrict非依存のcore helperから構築する。

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:53-55`

```python
MEASUREMENT_DATE = "2026-07-22"
STRICT_OFF = {"strict_gt_conformance": False}
ALL_CONFIGS = ("N1", "N2", "N3", "N3-std")
```

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:113-122`

```python
t3_samples, scene_samples = [], []
views_by_sid = core.run_supreme_scenarios(
    train_snaps, params=None, config=STRICT_OFF
)
for sid, snaps in train_snaps.items():
    snaps = list(snaps)
    gt_views = list(train_gt.get(sid, []))
    views = views_by_sid[sid]
    t3_samples.append(core._t3_practice_from_scenario(snaps, views, gt_views))
    scene_samples.append(core._scene_practice_from_scenario(snaps, gt_views))
```

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:364-377`

```python
def build_t2_scens(train_snaps, train_gt):
    """core.fit_supreme(core.py:1611-1622)と同一手順で T2 学習入力 [(feats, gts)] を作る。"""
    sids_all = sorted(train_snaps.keys())
    stride = max(1, len(sids_all) // core._T2_FIT_MAX_SCENARIOS)
    t2_scens = []
    for sid in sids_all[::stride][:core._T2_FIT_MAX_SCENARIOS]:
        snaps_s = list(train_snaps[sid])
        gt_views = list(train_gt.get(sid, []))
        feats = core._neupsl_inputs_from_scenario(snaps_s)
        gts = [{"mode": (gt_views[i] or {}).get("t2_mode") if i < len(gt_views) else None,
                "role": (gt_views[i] or {}).get("t2_role") if i < len(gt_views) else None,
                "rel": (gt_views[i] or {}).get("t2_relation") if i < len(gt_views) else None}
               for i in range(len(feats))]
        t2_scens.append((feats, gts))
```

**判定**: 実装済

**コメント**: t3→scene→T2の順序と既定値比較ガードは `:124-169` でcoreの学習順を再構成している。evalも `:461` で `config=STRICT_OFF` を明示する。

### F-015-6: 決定性

列挙、preflight、整形、実走、採点を乱数・時刻から独立させ、同一paramsで2回のview一致を確認する。

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:528-540`

```python
def determinism_check(params, recs_eval_nonviol, pso_cache):
    sub = [r for r in recs_eval_nonviol if r["suite"] == "std"][:5]

    def _run():
        out = {}
        for r in sub:
            snaps = sc.prepare_snaps(pso_cache[r["sid"]])
            out[r["sid"]] = core.run_supreme_scenarios(
                {r["sid"]: snaps}, params, config=STRICT_OFF)[r["sid"]]
        return out

    a, b = _run(), _run()
    return {"subset": [r["sid"] for r in sub], "identical": (a == b)}
```

**判定**: 実装済

**コメント**: 対象コードの静的検索では乱数APIの使用はない。`time.perf_counter()`はfit/eval秒数のmetadataにだけ使用される。既存 `results.json` では全4 configが `identical: true` だが、確認対象は各config 5本に限定される。

### F-015-7: cache・分割実行の provenance

cache manifestにkey、engine HEAD、data root/HEAD、レシピIDを保持し、完全一致時のみ再利用する。旧T2は再生成内容一致時だけ移行し、strict ON時代のt3sceneを再利用しない。resultsマージ時はconfigごとのprovenanceを検証する。

**実現箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:175-204`

```python
def cache_manifest(key, engine_head, data_root, data_head, recipe_id=LEARNING_RECIPE_ID):
    """cache 値の出所を固定する manifest を作る。"""
    return {
        "schema_version": CACHE_SCHEMA_VERSION,
        "key": key,
        "engine_repo_head": engine_head,
        "data_root": os.path.abspath(data_root),
        "data_repo_head": data_head,
        "learning_recipe_id": recipe_id,
    }

def _cache_blob_matches(blob, expected_manifest):
    return (
        isinstance(blob, dict)
        and "value" in blob
        and blob.get("manifest") == expected_manifest
    )
```

旧t3sceneの不一致削除は同ファイル `:282-295`、旧T2入力の内容比較とmanifest付与は `:321-357` に実装されている。

**不足箇所**: `reports/situations_v1-eval-20260722/run_supreme_situations.py:589-600`

```python
legacy_meta = results.get("meta", {})
mismatches = []
for name, config_result in sorted(results.get("configs", {}).items()):
    provenance = config_result.get("provenance")
    if provenance is None:
        provenance = result_provenance(
            legacy_meta.get("engine_repo_head"),
            legacy_meta.get("data_root", current_provenance["data_root"]),
            legacy_meta.get("data_repo_head"),
        )
        config_result["provenance"] = provenance
    if provenance != current_provenance:
        mismatches.append({"config": name, "provenance": provenance})
```

**判定**: 部分的に実装

**コメント**: 通常のconfig別provenance不一致は`:603-608`で既定拒否され、`--force-mixed`も実装されている。ただし既存configとlegacy metaの両方に`data_root`がない場合、保存値を検証せず現在の`data_root`を補って一致扱いにできる。仕様の「保持するconfigごとのdata rootを検証」を満たさない。欠落provenanceは既定拒否し、`--force-mixed`時だけ保持すべきである。

## テストカバレッジ

- テスト定義: `tests/test_F015_situations_adapter.py` に33件。うち2件は外部situations_v1ルートがない場合skip
- providerのread-only規約上、`tmp_path`利用テスト等がファイルを作成するためpytestは実行していない

テストされている振る舞い:

- clean、bad_version、ts後退/同値、type_break、フレーム数不一致: `tests/test_F015_situations_adapter.py:76-168`
- version非改変、min_TTC_sだけの補完: `:174-211`
- GTの8層変換、hazard/dynamics除外: `:217-250`
- index対応とview/GT長不一致拒否: `:256-269`
- 非違反trainのfalse reject除外: `:287-308`
- 学習用t3 mode系列のstrict OFF透過: `:311-343`
- runnerでのview/GT長不一致incident: `:346-368`
- cache manifest不一致時の再計算、旧T2再移行: `:371-412`
- config別provenance不一致の既定拒否とforce: `:415-430`
- 外部データがある場合の720本件数とeval違反拒否: `:440-468`

テストされていない振る舞い:

- manifest分岐とdir walk fallbackの合成テスト、fallback時の違反フラグ復元
- pyyaml不在時の明示停止
- 複数違反同時発生時の固定優先順位、`other` verdict、`tracks.objects`の個別境界
- 契約違反シナリオでengine/fitが一度も呼ばれないこと
- `rejection_tally`のeval/train別計算と8層分母除外
- eval engine呼び出しへのstrict OFF透過
- `fit_supreme_strict_off`全体とT2入力helperの配線
- engine例外時のsid・トレースバックincident
- `score_trace`がpooledと全suiteについて実際に`harness.score`を呼ぶこと
- 全235本および学習paramsを含むend-to-end決定性
- strict ON時代のt3scene削除、legacy T2不一致時の破棄/停止
- provenance欠落、特に保存済み`data_root`欠落時の拒否
- `make_report.py`によるREADME生成とresults.jsonとの数値一致

テスト規約上の不足:

- `specs/TEST_STRATEGY.md:36-38` はテスト名への受け入れ条件IDと、各テストdocstring冒頭へのID記載を必須としている。
- F-015テストは `test_preflight_*` 等の名前で、F-015-1〜7との対応docstringがないため、受け入れ条件とテストの機械照合ができない。

## アーキテクチャ上の位置づけ

- 実現したノード: `sitload`, `sitrun`, `sitreport`
- 利用する下流ノード: `core`, `harness`
- このノード群に対する充足度: 部分
- 根拠:
  - `specs/ARCHITECTURE.md:33-36` に3ノードが定義されている
  - `:63-66` の `sitload → sitrun → core/harness/sitreport` は実コードのimport、engine実走、採点、results出力と一致する
  - `:153-158` の責務表も実コードと一致する
- 上流（入力元）の未実装: なし。外部situations_v1データルートはリポジトリ外依存であり、既存結果に720本の読込実績がある
- 下流（出力先）の未実装:
  - core実走、harness採点、results.json、README.md生成コードは存在する
  - `sitrun`内のresults provenance検証にF-015-7記載の不足が残る
- 図とコードのズレ: なし
- 管理メタデータ上の軽微なズレ:
  - `sitreport`のREADME生成を実装する `reports/situations_v1-eval-20260722/make_report.py` が、`specs/status.json:125` のF-015 `files`一覧に含まれていない

## オーバー実装の検出

仕様にない実装: なし。

N1/N2/N3/N3-std、段階cache、旧cache移行、`--force-mixed`はSPECまたはADR 0058で明示されており、オーバー実装とは判定しない。

## 推奨アクション

- 優先度P1: `_load_existing`でconfig別provenanceまたは必須キーが欠落している場合、現在値を補わず既定拒否する。`--force-mixed`指定時だけ保持を許可する
- 優先度P1: 契約違反でengine/fitが呼ばれないこと、rejection分母除外、eval strict OFF、harnessのpooled/per-suite呼び出しをmockで検証する
- 優先度P1: engine例外、strict ON t3scene破棄、legacy T2不一致、provenance欠落の異常系テストを追加する
- 優先度P2: F-015-1〜7とテストの対応ID/docstringをTEST_STRATEGY規約どおり明示する
- 優先度P2: `make_report.py`をF-015の実装ファイル一覧に反映し、results.jsonとREADME.mdの整合テストを追加する
