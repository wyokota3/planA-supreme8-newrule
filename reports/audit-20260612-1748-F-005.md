# 監査レポート: F-005
日時: 2026-06-12 17:48
対象: F-005 (baseline 取込＋エラー分析 / `erroran` ノード)

## サマリ
- 受け入れ条件: 全2件中、実装済2件、未実装0件
  - F-005-1（弱い5項目の誤りが項目別・クラス別に出力）: 実装済
  - F-005-2（各改良モジュールが参照する構造原因の文書化・骨子）: 実装済
- 追加観点の判定:
  - 境界条件（突合可能な場合のみ取込・突合ルール）: 実装済
  - 異常系（U22・想定外形式は黙って読み飛ばさず報告）: 実装済
  - F-008 注記の遵守（配線漏れ仮説を事実としてコード化しない）: 遵守
- オーバー実装: なし（仕様逸脱に該当する実装は検出せず。後述の所見1件は仕様範囲内と評価）
- リスク: 低
  - 根拠: 受け入れ条件2件はいずれも実装コードで行番号引用可能。境界条件・異常系・F-008 注記の重点観点も満たす。残る限界は「実GTでの再検証は未実施（dict 入力前提・設計どおり）」だが、これは SPEC/ADR が F-005 を「dict を受ける／実GT 再検証は研究者の手動運用」と定義した範囲内であり、機能の欠落ではない。

---

## 受け入れ条件の実現状況

### F-005-1: 弱い5項目それぞれについて、誤りの所在が項目別・クラス別に出力される

弱い5項目の定義は `src/supreme/erroran.py:64-67`。

```python
# 分析必須の弱い5項目。
WEAK_LAYERS = (
    "t2_mode", "t2_relation", "t3_hypothesis", "quality_regime", "scene_regime",
)
```

GT_SCHEMA.md「9評価項目との対応」(GT_SCHEMA.md:84-94) の弱い5項目（mode/relation/T3/Scene/Quality）と完全一致する。

**実現箇所（項目別・クラス別の出力）**: `src/supreme/erroran.py:104-152`（`AnalysisResult`）。

- クラス別の混同行列 `src/supreme/erroran.py:116-127`:
```python
    def confusion_matrix(self, layer):
        cm = {}
        for fr in self._frames.get(layer, []):
            gt = fr["gt"]
            pred = fr["pred"]
            cm.setdefault(gt, {})
            cm[gt][pred] = cm[gt].get(pred, 0) + 1
        return cm
```
GT クラス×予測クラスの件数を入れ子 dict（`dict[gt][pred]=int`）で返し、「どのクラスをどのクラスと誤ったか」をクラス別に出力する。

- 誤りフレーム一覧 `src/supreme/erroran.py:129-143`（`{scenario_id, ts, pred, gt}`、gt≠pred のみ）。
- 正解率 `src/supreme/erroran.py:145-152`（フレーム0件で 1.0 を返す境界処理あり）。

**項目別の蓄積（誤りの定義）**: `src/supreme/erroran.py:359-374`（`analyze`）。view（予測）と gt（正解）の差を層別に蓄積する。

```python
    per_layer_frames = {layer: [] for layer in WEAK_LAYERS}
    for scenario_id, frames in trace.items():
        for frame in frames:
            ts = float(frame["ts"])
            view = frame["view"]
            gt_labels = frame["gt"]
            for layer in WEAK_LAYERS:
                per_layer_frames[layer].append({
                    "scenario_id": scenario_id, "ts": ts,
                    "gt": gt_labels[layer], "pred": view[layer],
                })
    return AnalysisResult(layers=list(WEAK_LAYERS), per_layer_frames=per_layer_frames)
```

**判定**: 実装済
**コメント**: 弱い5項目すべてが `WEAK_LAYERS` で網羅され、各項目に対して「クラス別混同行列・誤りフレーム一覧・正解率」が取得できる。誤りの定義は trace の view（baseline 予測）と gt（正解）の差。GT_SCHEMA の弱い5項目定義と一致。

### F-005-2: 各改良モジュール（F-007〜011）が着手前に参照すべき構造原因が文書化される（骨子）

**実現箇所**: `src/supreme/erroran.py:420-491`（`generate_report`）。

- 弱い5項目セクション `src/supreme/erroran.py:446-467`: 各 `WEAK_LAYERS` について「統計（accuracy・フレーム件数/誤り件数）」「混同行列」「主要誤りパターン」「原因仮説（記入欄）」を出力する。記入欄は `src/supreme/erroran.py:464-467`:
```python
        parts.append(
            "**原因仮説（記入欄）**\n"
            "- TODO（記入欄）: この項目の構造原因の仮説を記入する。\n"
        )
```
- F-008 配線漏れ仮説の検証セクション `src/supreme/erroran.py:469-489`: relation の誤りパターン statistics（accuracy・件数・誤りパターン箇条書き）と判定記入欄を出力する。

**判定**: 実装済
**コメント**: TEST_STRATEGY 引用（test_F005_report.py:14）に「内容の十分性はテスト対象外。骨子に5項目全セクションが存在すること」とあり、SPEC.md F-005-2 も「骨子」を求める。実装は弱い5項目の全セクション＋F-008 検証セクション＋記入欄を生成しており、骨子の要件を満たす。構造原因そのもの（U3）はレポートの記入欄に委ねられており、これは仕様どおり（U3 は F-005 完了時に確定する未決定事項であり、コードが断定しない方が正しい）。

---

## 境界条件: baseline 側GTと F-001 スキーマが突合できる場合のみ取込（突合ルール）

SPEC.md F-005 境界条件（SPEC.md:127）と GT_SCHEMA.md「9評価項目との対応」(GT_SCHEMA.md:84-94) を実装で照合した。

**突合ルール（argmax集合包含 / 完全一致）の実装**: `src/supreme/erroran.py:72-78`（対応表）、`src/supreme/erroran.py:242-265`（判定ロジック）。

- argmax 集合包含（t2_mode / t2_relation）: `src/supreme/erroran.py:242-247`（`_argmax_set`）＋ `src/supreme/erroran.py:261-263`:
```python
    if method == "argmax":
        matched = trace_gt_label in _argmax_set(node)
        return matched, node
```
`_argmax_set` は最大値を取る全クラスの集合を返す（`src/supreme/erroran.py:247`: `{cls for cls, v in dist.items() if v == max_val}`）ため、同率最大が複数あるときも「集合包含」で判定する。これは SPEC 指定（t2系=argmax集合包含）に一致。test_F005_ingest.py の tie テスト2件（行110-130, 133-154）が同率最大の包含/非包含を検証している。

- 完全一致（t3_hypothesis / quality_regime / scene_regime）: `src/supreme/erroran.py:264-265`:
```python
    else:  # string
        return node == trace_gt_label, node
```
SPEC 指定（t3/regime系=完全一致）に一致。

- 対応先フィールドのパス `src/supreme/erroran.py:72-78` は GT_SCHEMA.md:84-94 と一致:
  - `t2_mode` → `("t2","mode")`、`t2_relation` → `("t2","relations")`、`t3_hypothesis` → `("t3","hypothesis")`、`quality_regime` → `("t3","quality_regime")`、`scene_regime` → `("t3","scene_regime")`。

**フレーム対応キー (scenario_id, ts) と「突合できる場合のみ」**: `src/supreme/erroran.py:272-333`（`ingest`）。
- canonical 索引化 `src/supreme/erroran.py:228-239`（`datagov.normalize` 経由・キー `(sid, float(ts))`）。datagov.normalize は deep copy を返す冪等正規化（datagov.py:400-406）で、設計どおり supreme 内部リンクで再利用されている。
- trace にあって canonical に無い → `missing_frames`（`src/supreme/erroran.py:301-304`）。
- canonical にあって trace に無い → `extra_frames`（`src/supreme/erroran.py:321-324`）。
- 不整合は全件 `mismatches` に記録（`src/supreme/erroran.py:312-319`、early-return せず全層・全フレームを走査）。
- 「突合できる場合のみ取込（＝分析）」の強制は `analyze` の `src/supreme/erroran.py:350-357`:
```python
    result = ingest(trace, canonical_records)
    if not result.ok:
        raise IngestError("trace と canonical GT が突合できないため分析を中止", ...)
```
`ok` の定義は `src/supreme/erroran.py:326`（`not (mismatches or missing_frames or extra_frames)`）。

**判定**: 実装済
**コメント**: 突合ルール（t2系=argmax集合包含、t3/regime系=完全一致）、フレーム対応キー (scenario_id, ts)、「突合できる場合のみ分析する」の強制が全て実装され、行番号引用可能。GT_SCHEMA.md の突合キー定義（GT_SCHEMA.md:77）とも一致。

---

## 異常系: 結果ファイルが想定形式でない場合は報告（U22）

SPEC.md F-005 異常系（SPEC.md:126）「黙って読み飛ばさない」、ADR 0005（U22 解決）を実装で照合した。

**実現箇所**: `src/supreme/erroran.py:163-221`（`_validate_trace_format`）＋例外 `TraceFormatError`（`src/supreme/erroran.py:33-37`）。`ingest` 冒頭 `src/supreme/erroran.py:283` で必ず呼ばれる。

検査項目と test_F005_abnormal.py の対応:
- trace 非 dict（行273-287）→ `src/supreme/erroran.py:173-176`。
- 空 trace（行212-227）→ `src/supreme/erroran.py:177-178`。
- シナリオ値が list でない / dict（行181-205）→ `src/supreme/erroran.py:182-186`。
- フレーム非 dict → `src/supreme/erroran.py:188-191`。
- 必須キー ts/view/gt 欠落（行77-115）→ `src/supreme/erroran.py:192-197`。
- ts 非数値（文字列/None/bool）（行121-143）→ `src/supreme/erroran.py:159-160`（`_is_number`、bool 除外）＋ `src/supreme/erroran.py:198-202`。
- view/gt の未知層キー（行149-174）→ `src/supreme/erroran.py:209-215`。
- view/gt の層キー欠落（行294-335）→ `src/supreme/erroran.py:216-221`。
- 空シナリオ（空フレームリスト）（行246-265）: `_validate_trace_format` は空 list を通過させるが、ingest で canonical 側フレームが `extra_frames` に入り `ok=False` になる（`src/supreme/erroran.py:321-324`）。テストは「ok=False または extra_frames に sc1 が報告」を許容しており充足。

**判定**: 実装済
**コメント**: U22 が要求する「想定外形式は黙って読み飛ばさず例外/明示報告」を、形式不正は `TraceFormatError`（明示例外）、突合不能は `IngestResult.ok=False`＋構造化フィールド（`mismatches`/`missing_frames`/`extra_frames`）で実現。test_F005_abnormal.py の全 8 観点に実装箇所が対応する。ファイルI/O・再実行はシステム要件外（ADR 0005・SPEC.md:419）であり、本モジュールが dict 入力を受ける設計と整合する。

---

## F-008 注記の遵守（重点確認）

SPEC.md F-008 注記（SPEC.md:158）・U3（SPEC.md:321-324）は「relations 配線漏れは未検証の仮説。実装者は仮説を事実として直接コード化しない」と規定する。

**確認結果: 遵守している。**

- レポートの F-008 セクションは仮説を「未検証」と明示し、統計のみ提示する（`src/supreme/erroran.py:474-489`）:
```python
    parts.append("## F-008 relation 配線漏れ仮説の検証\n")
    parts.append(
        "> 仮説（未検証・SPEC.md F-008）: relations が mode ルールの副作用として"
        "各1箇所でしか発火せず、抽出済み証拠が relation に配線されていない。\n"
    )
    ...
    parts.append(
        "**判定（記入欄）**\n"
        "- [ ] 配線漏れ仮説は支持される / [ ] 棄却される（verdict）\n"
        "- TODO（判定記入欄）: 上記 statistics を根拠に判定を記入する。\n"
    )
```
- レポート冒頭にも注記がある（`src/supreme/erroran.py:440-443`）。
- 判定（verdict）は空のチェックボックス `[ ]`＋TODO 記入欄であり、コードは支持/棄却のどちらにも値を埋めていない。
- 配線漏れを前提とした分岐・閾値・relation 専用の特別扱いはコード全体に存在しない。relation は他の弱い項目と同じく `WEAK_LAYERS` の一要素として一様に統計集計されるのみ（`src/supreme/erroran.py:307-319`, `359-374`, `470-484`）。
- ソースコメントでも「本レポートはセクションと統計を提供するのみで、仮説を事実として断定しない」と明記（`src/supreme/erroran.py:429-430`）。

**判定**: 遵守。レポートは統計＋記入欄に留まり、配線漏れ仮説を事実としてコード化していない。test_F005_report.py の F-008 テスト（行180-228）も docstring で「セクションの存在のみ確認し仮説の真偽は確認しない」と明示しており、テストも仮説をコード化していない。

---

## テストカバレッジ

テストは4ファイル。docstring ID と受け入れ条件の対応を照合した（再実行はしていない。静的突合）。

### テストされている振る舞い
- F-005-1（取込・突合）: test_F005_ingest.py
  - 正常系 ok（行45-58）、view≠gt でも gt 整合なら ok（行60-71）
  - t2_mode argmax 不整合拒否（行77-108）、tie 包含で合格（行110-131）、tie 非包含で拒否（行133-155）
  - t2_relation argmax 不整合拒否（行161-181）
  - t3_hypothesis / quality_regime / scene_regime 文字列不一致拒否（行187-235）
  - missing_frames / extra_frames 報告（行242-285）、複数不整合の全件報告（行288-309）
- F-005-1（分析・項目別/クラス別出力）: test_F005_erroran.py
  - 弱い5項目の全層存在（行57-64）
  - 混同行列の構造・既知件数（t2_mode/t2_relation/quality_regime/scene_regime の各1件、t3_hypothesis 誤り0）（行71-167）
  - 誤りフレーム一覧の構造・既知フレーム・件数・全正解時空（行174-235）
  - accuracy の完全一致時1.0・既知期待値 5/6・[0,1] 範囲（行242-307）
  - 混同行列セル合計＝フレーム総数 6（行314-323）
- F-005-2（レポート骨子）: test_F005_report.py
  - str 返却（行60-65）、弱い5項目全セクション存在（行71-119）、統計（acc/混同行列/誤りパターン）言及（行126-154）、原因仮説記入欄（行161-173）、F-008 セクション存在/統計/判定記入欄（行180-228）、全正解 trace でも生成（行235-246）
- F-005 異常系（U22）: test_F005_abnormal.py（必須キー欠落・ts 非数値・未知層キー・非 list・空 trace・空シナリオ・非 dict・層キー欠落の 8 観点、行77-336）

### テストされていない振る舞い
- `IngestError` 例外オブジェクトに保持される `mismatches`/`missing_frames`/`extra_frames` 属性（`src/supreme/erroran.py:46-51`）の値を `analyze` 経由で直接検証するテストが無い。test_F005_ingest.py は `ingest` 戻り値か `ingest` が送出する例外を検証するが、`analyze` が `ingest` 不整合時に送出する `IngestError`（`src/supreme/erroran.py:352-357`）の属性内容を assert するテストは存在しない（`analyze` の不整合経路は test_F005_abnormal.py:229-239 で例外送出のみ広く許容され、属性検証なし）。
- `IngestResult.warnings` フィールド（`src/supreme/erroran.py:101`、現状常に空）の内容を検証するテストが無い（test_F005_abnormal.py は warnings を「許容される報告手段の一つ」として参照するのみ。`src/supreme/erroran.py` は warnings を空のまま返すため、未知層キーは TraceFormatError 経路で扱われ warnings は使われない）。
- `confusion_matrix` の対角成分（正解クラス）件数そのものの値検証は test_F005_erroran.py:154-167 が off-diagonal=0 を確認するに留まり、対角の具体的件数（例: conv_request→conv_request の件数）を assert するテストは無い（セル合計＝6 の検証で間接的にカバー）。
- レポートの数値フォーマット（accuracy の `.4f` 表示・混同行列 Markdown 表）の正確な書式検証は無い（存在確認・数字含有確認に留まる。骨子テストの方針上、内容十分性はテスト対象外と明示されているため設計どおり）。
- 浮動小数 ts のキー一致（trace `ts` と canonical `ts` を `float()` で正規化して突合する `src/supreme/erroran.py:238, 296`）について、整数/浮動の混在や微小誤差の境界テストは無い（フィクスチャは 0.0/1.0/2.0 のみ）。

---

## アーキテクチャ上の位置づけ

- 実現したノード: `erroran`（ARCHITECTURE.md:19, 134・F-005）
- このノードに対する充足度: 部分（責務「baseline 結果を取込み、項目別にどこで・なぜ間違うかを抽出。改良の方向を構造原因から決める改良の入力源」(ARCHITECTURE.md:134) のうち、取込・項目別誤り抽出・構造原因レポート骨子を実装。「なぜ間違うか＝構造原因」の中身は U3 として未確定であり、レポートの記入欄に委ねる設計。これは仕様どおりで、機能の欠落ではない）
- 上流（入力元）の未実装:
  - baseline 結果 trace.json（外部 GitHub・`ddb1b97`、SPEC.md:50）の実ファイル読込は本モジュールに無い（dict 入力前提。ファイル取得は研究者手動運用＝ADR 0005・SPEC.md:419 で要件外）。設計どおりで欠落ではない。
  - canonical GT は F-001（`datagov`、監査済み done）を `datagov.normalize` 経由で再利用（`src/supreme/erroran.py:26, 230-238`）。上流 F-001 は実装済みで、突合の前提（GT 単一スキーマ）が成立している。
- 下流（出力先）の未実装:
  - erroran の下流エッジは「構造原因」を mode(F-007)/relation(F-008)/scene(F-010)/quality(F-011)/t3(F-009) へ供給（ARCHITECTURE.md:43-47）。これら下流ノードは全て未実装（枠のみ・SPEC.md:148-191、U1/U3 待ち）。これは仕様の意図どおり（F-005 完了が U1/U3 を確定させ、その後に下流着手）。erroran 側の出力（5項目別の統計＋構造原因レポート骨子）は下流が参照可能な形で揃っている。
- 図とコードのズレ: なし。
  - ARCHITECTURE.md:43-47 の erroran → {mode, relation, scene, quality, t3} の5本のエッジは「構造原因」を運ぶもの。コードは弱い5項目（`WEAK_LAYERS` = t2_mode, t2_relation, t3_hypothesis, quality_regime, scene_regime）を集計し、各項目のレポートセクションを生成する（`src/supreme/erroran.py:446-467`）。図の5つの下流ノードと、コードが扱う弱い5項目が一対一で対応する（t3 エッジ ↔ t3_hypothesis、relation エッジ ↔ t2_relation 等）。
  - 「relation には構造原因・仮説検証」と図が注記する点（ARCHITECTURE.md:44）も、コードの F-008 検証セクション（`src/supreme/erroran.py:469-489`）と一致。
  - erroran が `epiin`/`epiout`（supreme 本体の入出力契約）に接続していない点も図どおり（erroran は本体サブグラフ外の独立ノード）。

---

## オーバー実装の検出

指示された観点（ファイルI/O・外部クローン読込・YAML パーサが入っていないか、WEAK_LAYERS/TRACE_LAYERS 定数公開は仕様範囲か）で精査した。

- ファイルI/O: なし。`open`/`Path`/ファイル読込は `src/supreme/erroran.py` 全体に存在しない。入力は dict（モジュール docstring `src/supreme/erroran.py:8-9` の宣言どおり）。仕様逸脱なし。
- 外部クローン読込: なし。GitHub クローンへのアクセスコードは無い（ADR 0005・SPEC.md:419 のとおり研究者手動運用）。仕様逸脱なし。
- YAML パーサ: なし。`import yaml` 等は無い。datagov の `normalize`（deep copy）のみ再利用。仕様逸脱なし。stdlib（dataclasses）のみに依存（`src/supreme/erroran.py:22-26`）。
- `TRACE_LAYERS` 定数公開（`src/supreme/erroran.py:58-62`）: 8層キーは ADR 0005 記録事項（ADR 0005:46-49・8層で Anomaly 無し）と SPEC.md:50, 420 に明記された評価層。形式検証（view/gt が8層ちょうどを持つこと）に必須であり、仕様の 8層定義をコード化したもの。仕様範囲内。
- `WEAK_LAYERS` 定数公開（`src/supreme/erroran.py:64-67`）: SPEC.md:17, 453・GT_SCHEMA.md:84-94 の弱い5項目をコード化したもの。F-005 の分析対象そのもの。仕様範囲内。
- `IngestResult.warnings` フィールド（`src/supreme/erroran.py:101`）: 現状常に空（未使用）。docstring（`src/supreme/erroran.py:94`）は「黙殺しない補助情報・現状未使用だが属性として公開」と明示。test_F005_abnormal.py が warnings を「許容される報告手段の一つ」として参照する（行165-171, 308-314）ためインターフェースとして用意されており、テスト契約に沿った範囲。死蔵フィールドではあるが仕様逸脱ではない（所見として記録、オーバー実装には分類しない）。

**結論**: 仕様にない実装（オーバー実装）は検出されなかった。指示で懸念された I/O・外部読込・YAML はいずれも不在で、SPEC/ADR の「dict を受ける／stdlib のみ／手動運用」の方針を厳守している。

---

## 推奨アクション

優先度・低（いずれも機能の欠落ではなく、堅牢性・検証網羅の補強）:

1. （低）`analyze` が突合不整合時に送出する `IngestError` の属性（`mismatches`/`missing_frames`/`extra_frames`）内容を assert するテストを追加すると、`src/supreme/erroran.py:352-357` の経路が直接カバーされる。現状は `ingest` 経路でのみ属性が検証されている。
2. （低）`IngestResult.warnings`（`src/supreme/erroran.py:101`）は現状常に空で死蔵。将来 warnings を使う具体予定（例: 分布合計が 1±0.01 を外れる baseline 入力の補助警告）が無ければ、フィールドの存在意図をコメントで「予約」と明記済みである点を維持し、使う段で初めてテストを追加する方針で問題ない。
3. （低）浮動小数 ts の突合（`src/supreme/erroran.py:238, 296` の `float()` 正規化）について、整数 ts と浮動 ts の混在ケースのテストを足すと回帰防止になる（現フィクスチャは整数値の float のみ）。

注: 上記はいずれも実装変更を伴わない（1・3 はテスト追加、2 は現状維持の確認）。F-005 の受け入れ条件・境界条件・異常系・F-008 注記はすべて充足しており、本監査の範囲で仕様違反・オーバー実装・図とコードのズレは検出されなかった。
