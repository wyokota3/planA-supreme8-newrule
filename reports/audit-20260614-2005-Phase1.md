# 監査レポート: Phase 1（学習モジュールの core 配線・ADR 0025）

日時: 2026-06-14 20:05
対象: `core.fit_supreme` / `SupremeParams` / `run_supreme(params=)` 配線 ＋ scene `_LEARNABLE_PARAM_NAMES` 9→3 訂正 ＋ sealeval config keyword 化
監査種別: 静的突合（ADR 0025 × 実装 × テスト）。read-only。758テスト全緑・dev-eval 既定経路不変はオーケストレーター独立確認済み。

## サマリ
- 受け入れ条件（ADR 0025 決定1〜4 / テスト観点1〜6）: **全6観点 実装済**。
- オーバー実装: なし（配線は t3/scene のみ・決定2 と一致）。
- リスク: 低。**done 可（条件なし）。**

## 最優先 judgment: scene `_LEARNABLE_PARAM_NAMES` 9→3 = 正当な訂正（gaming ではない）
- `scene.fit` は **閾値3個（vol_high/persist_high/level_low）のみ**を座標降下で更新し、**HGF 6個は `default_hgf_params()` で固定**（`scene.py:414,449-467`）。
- 旧宣言9（HGF6+閾値3）は fit 実態（3更新）と**不整合**だった（`run_cv_train.py:675-677` が旧状態を裏取り）。9→3 はこの不整合を fit 実態へ一致させる訂正。
- **U24/ADR 0018:32-35**「学習可能=学習で更新される連続値のみ計上・固定ルール閾値定数は数えない」に厳密準拠 → 固定 HGF は過学習容量を増やさないので **3 が honest・9 が過大**。
- F-010/F-009 budget テストは個数を**明示委任**（`test_F010_budget.py:20-22` 範囲のみ・代表値9はハードコードで `learnable_param_count()` 非依存）→ 9→3 でテスト非破壊。
- gaming 仮説（「9だと Phase1 予算テストが落ちるから下げた」）を逆向き検証 → 計数の根拠は U24 と fit 実態であり、テスト合格は「正しい計数にしたら結果的に通った」。**gaming ではない**。

## 受け入れ条件
- 観点1 後方互換（最重要）: `_run_one_scenario(snaps, config, params=None)` で `params is None` → `learned_scene=None`・`t3_params=default_params()`（`core.py:532,550`）＝旧経路と同一。`run_supreme(snaps)==run_supreme(snaps,params=None)` を完全一致で固定。**実装済**。
- 観点2 公開面（`SupremeParams`/`fit_supreme`・`core.py:678-697,800-857`）: 実装済。
- 観点3 決定性（fit/run とも乱数・時刻なし）: 実装済。
- 観点4 学習済み実走で8層充足: 実装済。
- 観点5 in-sample ≥ 既定: `fit_supreme` が学習結果と既定を train acc で比較し良い方を採用（`core.py:831-855`）。過適合でなく「学習が既定を下回らない」担保（`>=`）。健全。
- 観点6 F-014 計上（`learnable_param_count`=t3 6+scene 3=9・fit 前後不変）: 実装済。

## fit_supreme の抽出が core 実経路と一致
`core._t3_practice_from_scenario`/`_scene_practice_from_scenario`（`core.py:700-751`）は `run_cv_train.py:83-117` と同一抽出。`fit_supreme` は実 view を `_run_one_scenario(snaps,None,None)`（=既定経路）で生成（`core.py:827`）。**一致**（運用入力と乖離しない）。

## 後方互換（シグネチャ）
`run_supreme(pso_snapshots, params=None, config=None)`。既存呼び出しに `config` を**第2位置**で渡すものは皆無（全て keyword `config=...` or 1引数）。sealeval は `config=config`（keyword）。**破れなし**。

## 残存指摘（いずれも done を妨げない・低）
| ID | 内容 | 対処 |
| --- | --- | --- |
| P1-R1 | HANDOVER.md の `run_supreme` 署名が `params=` 未反映 | 文書更新（本完了で対処） |
| P1-R2 | Phase1 予算テストの data=フレーム数(24)が SPEC.md:252「練習用シナリオ件数」と定義不一致（本 fixture では合格・将来 fixture 変更時の誤判定リスク） | ADR/コメントで明示（記録） |
| P1-R3 | 「fit が HGF を更新しない=計上3」を固定する直接テストが無い（将来 HGF 学習化時の計上漏れ回帰検出なし） | 任意 |
| P1-R4（重要・将来） | `sealeval.run_sealed_evaluation` は学習済み params を注入していない（`config` 省略=既定）。**封印 verdict に学習を効かせるには別途配線が要る**（Phase 1b） | 申し送り |

## 総合
Phase 1 の配線（decision1）・対象限定（decision2）・決定性/予算（decision3）・後方互換（decision4）すべて実装で確認。scene 9→3 は U24 と fit 実態に基づく正当な訂正。**done 可（条件なし）**。P1-R4（封印評価への学習注入）は本 Phase スコープ外の将来作業。

AUDIT_VERDICT: pass
