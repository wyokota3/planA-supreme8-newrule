# 開発フロー: 役割分離型マルチエージェント開発

## このドキュメントの目的

AIに思考の射程を超えさせつつ、自分が意思決定を握り続けるための開発フロー。
実装の細部はAIに任せ、人間は以下の4点を握る:

1. SPEC.md と TEST_STRATEGY.md のレビュー
2. 機能分割の判断
3. 各ステップの承認
4. ADR(設計判断)への記録

---

## フロー全体像

```
[フェーズ0] アーキテクチャ議論
  └─ ChatGPT または Claude チャットで対話的に設計

[フェーズ1] 引き継ぎ書生成
  └─ プロンプト1を使って SPEC.md と TEST_STRATEGY.md を生成

[フェーズ2] 環境構築
  └─ Claude Code 新規セッションでプロンプト2を実行
  └─ 役割分離型サブエージェント環境を構築

[フェーズ3] 実装ループ
  └─ Claude Code 新規セッションでプロンプト3を実行
  └─ 機能単位で8ステップループを回す
```

---

## フェーズ0: アーキテクチャ議論

ChatGPT または Claude チャットで、作りたいシステムについて議論する。
この段階で意識すること:

- 結論だけでなく、却下した選択肢とその理由も会話に残す
- 「決まらなかったこと」を曖昧にせず、明示的に保留する
- 議論が発散したら一度まとめる、を繰り返す

議論が一段落したら、フェーズ1に進む。

---

## フェーズ1: 引き継ぎ書生成

### プロンプト1(ChatGPT または Claude チャット用)

フェーズ0の議論の続きで、以下を貼る。一発で出力させず、段階的に進める。

```
これまでの議論をClaude Codeへの引き継ぎ書にまとめたい。
一発で出力せず、以下の手順で段階的に作っていく。
各段階で私のレビューを待ってから次に進むこと。

## 段階1: 議論の棚卸し
この会話を最初から読み返し、以下を網羅的に箇条書きで出力:

1. 「決まったこと」リスト
   - 各項目に、議論のどのあたりで決まったかを一言で付記
   - 「なんとなく合意した」程度のものも漏らさず含める

2. 「検討したが却下したこと」リスト
   - 却下案と、却下理由をセットで
   - 「議論には出たが選ばなかった」ものを全て

3. 「決まらなかったこと/曖昧なまま進んだこと」リスト
   - 議論で触れたが結論が出ていない論点
   - 「後で考える」と保留した項目
   - 暗黙の前提として扱われたが明文化されていない事項

4. 「議論に出ていない重要論点」リスト
   - このシステムを実装する上で本来決めるべきだが、
     議論で触れられなかった項目
   - 例: エラーハンドリング方針、ログ設計、テスト戦略、
     データ移行、運用監視など

出力したら止まる。私が「次へ」と言うまで先に進まない。

## 段階2: 構造化
段階1のリストを以下の章立てに振り分け、不足を補う。
振り分けた結果、議論で曖昧だった項目は「未決定事項」章に
明示的に残すこと。勝手に埋めない。

# プロジェクト概要
- 一行サマリ
- 解決する問題(議論で確認した課題認識を再記述)
- スコープ内/スコープ外
- 想定ユーザーと利用シーン

# アーキテクチャ
- 技術スタック(言語/フレームワーク/DB/インフラ)
  各選択に、議論で出た選定理由を併記
- システム構成図(テキストで階層を表現)
- 主要コンポーネントと責務(各コンポーネント3-5行で)
- データフロー(主要なユースケース2-3個について、
  どのコンポーネントを通るか)
- 外部依存(API、ライブラリ、サービス、認証基盤など)

# 機能要件
各機能を以下の形式で:
## F-XXX: [機能名]
- 概要: 2-3行
- アクター: 誰が使うか
- 入力: 何を受け取るか(型・形式・制約)
- 出力: 何を返すか(型・形式・制約)
- 正常系の振る舞い: ステップで
- 異常系の振る舞い: 想定される失敗と対応
- 境界条件: 空入力、最大値、null、並行実行など
- 受け入れ条件: テストに変換可能な粒度で
  例) ✗「正しく動作する」
      ✓「金額0円のとき、明細にも合計にも含めない」
- 依存する他機能: F-XXX

# 非機能要件
- パフォーマンス: 具体的な数値目標(レスポンスXms以内など)
- スケール: 想定データ量、同時接続数
- セキュリティ: 認証/認可、機密データ、監査ログ
- 可用性: 想定稼働率、障害時の振る舞い
- 保守性: コードスタイル、ドキュメント、テストカバレッジ
数値が議論で出ていない項目は「未決定」と明記

# 設計判断の記録(ADR的に)
重要な選択について以下を記録:
## 判断N: [何を決めたか]
- 背景: なぜ判断が必要だったか
- 選択肢: 検討した案A、B、C
- 選んだもの: A
- 理由: なぜAを選んだか
- 却下した案の弱点: なぜB、Cを選ばなかったか
- リスクと将来の見直しトリガー: 何が起きたら再検討するか

# 未決定事項(最重要)
段階1で出た「決まらなかったこと」と「議論に出ていない重要論点」
を全て列挙。Claude Codeはこれらを勝手に解釈してはならない。

各項目に以下を付記:
- なぜ未決定か(情報不足/判断保留/優先度低 等)
- 実装のどの段階までに決める必要があるか
- 暫定的な扱い(あれば)

# 制約と前提
- 期日、予算、人員
- 既存システムとの互換性
- 法規制、社内規定
- 利用可能なライブラリやサービスの制約

# 用語集
議論で使われた専門用語、プロジェクト固有の語彙

出力したら止まる。次の段階に進む前に私のレビューを待つ。

## 段階3: 自己レビュー
段階2の成果物について、以下の観点で自己レビューせよ:

1. 「もし私(AI)がこの文書だけを読んで実装したら、
   何を間違えるか」を10個以上挙げよ。
   各項目について、文書のどこを補強すれば防げるかも併記。

2. 文書内で以下の曖昧表現を全て検出せよ:
   「適切に」「いい感じに」「など」「等」「必要に応じて」
   「柔軟に」「分かりやすく」「使いやすく」
   検出したら、議論内容から具体化できるか、
   未決定事項に移すかを判断せよ。

3. 受け入れ条件のうち、テストコードに変換できない
   抽象的なものを全て検出し、具体化案を提示せよ。

4. 機能間の依存関係に循環や矛盾がないか検証せよ。

5. 段階1で挙げた「議論で出たが却下した案」のうち、
   段階2で「却下理由」が記録されていないものを検出せよ。

レビュー結果を出力したら止まる。

## 段階4: 確定版の生成(SPEC.md)
段階3のレビュー結果を反映した最終版を、
SPEC.md として一つのMarkdownファイルにまとめる。
ファイル冒頭に、未決定事項の数とリスクレベルを
サマリとして記載すること。

## 段階5: テスト戦略の策定(TEST_STRATEGY.md)
SPEC.md とは別に、TEST_STRATEGY.md を作成する。
これはtest-writerサブエージェントへの指示書になる。

# テストレベルと責務
- ユニットテスト: 何を対象に、何を検証するか
- 統合テスト: コンポーネント境界のどこを検証するか
- E2Eテスト: どのユースケースをカバーするか
- 各レベルでカバーしないもの(意図的な不足)も明示

# テストフレームワークとツール
- 使用するフレームワーク(議論で決まっていれば、なければ提案)
- アサーションライブラリ
- モック戦略(何をモックし、何を本物で動かすか)
- テストデータの管理方法(フィクスチャ、ファクトリ等)

# カバレッジ目標
- 行カバレッジ、分岐カバレッジの目標値
- 「100%を目指さない領域」と、その理由
  (例: 外部APIラッパーの薄い層など)

# テスト命名規約
- ファイル名、テスト関数名、describe/it ブロックの規約
- 仕様ID(F-XXX)とテストの対応付けルール

# 各機能要件のテスト方針
SPEC.md の F-001 から順に、以下を記述:
## F-XXX のテスト方針
- ユニットテストで検証する観点
- 統合テストで検証する観点
- E2Eテストで検証する観点
- 境界条件として必ずテストすべきケース
- 異常系として必ずテストすべきケース
- テストが困難な部分と、その代替手段

# 非機能テスト
- パフォーマンステストの方針と閾値
- セキュリティテストの観点
- 負荷テストの方針(必要なら)

# テストの実行と維持
- ローカル実行の手順
- CIでの実行方針
- フレーキーテストへの対処方針
- テストが壊れたときの優先度判断基準

# 自己レビュー
最後に、「このテスト戦略の穴」を5個以上指摘せよ。
特に、SPEC.md の受け入れ条件のうち、
この戦略では捕捉できないものを明示せよ。

---

開始する。まず段階1から。
```

### フェーズ1終了時の成果物

- `SPEC.md` (仕様書)
- `TEST_STRATEGY.md` (テスト戦略)

この2ファイルを、これから作るプロジェクトのルートに置く準備をする。

---

## フェーズ2: 環境構築

Claude Code の新規セッションを開き、プロジェクトルートに `SPEC.md` と
`TEST_STRATEGY.md` を配置した状態で、以下を実行する。

### プロンプト2

```
これからこのプロジェクトに、役割分離型のマルチエージェント開発環境を
構築してほしい。実装はまだ始めない。環境構築だけに専念すること。

## 入力
- ./SPEC.md (仕様書)
- ./TEST_STRATEGY.md (テスト戦略)

## 作るもの

### 1. ディレクトリ構造
.claude/agents/ 配下に以下のサブエージェント定義を配置:
- spec-reviewer.md
- test-writer.md
- implementer.md
- auditor.md

./specs/ 配下に SPEC.md と TEST_STRATEGY.md を移動
./decisions/ を作成(ADRを置く)
./reports/ を作成(監査結果を置く)
./tests/ を作成(テストコードを置く)

### 2. CLAUDE.md(プロジェクトルート)
以下のルールを記載:
- 実装前に必ず ./specs/SPEC.md と ./specs/TEST_STRATEGY.md を読むこと
- 実装ワークフローは ./WORKFLOW.md に従うこと
- 実装は必ず implementer サブエージェント経由で行うこと
- メインセッション(オーケストレーター)はコードを書かない
- 仕様の曖昧な点に遭遇したら、勝手に解釈せずユーザーに確認する
- 機能単位で進め、一機能完了ごとに auditor を呼ぶこと
- 重要な設計判断は ./decisions/ にADR形式で記録すること

### 3. サブエージェント定義の内容

#### spec-reviewer.md
役割: 仕様書を読み、曖昧な点・矛盾・抜けを洗い出す
できること: 仕様書の読み込み、質問リストの作成
できないこと: コードを書く、仕様を勝手に補完する
ツール権限: Read のみ
出力形式: 質問・指摘リスト(Markdown)

#### test-writer.md
役割: 確定した仕様から受け入れテストを書く
できること: 仕様書・テスト戦略の読み込み、テストコードの読み書き、テスト実行
できないこと: 実装コードを見る・書く・変更する
ツール権限: Read, Write(./tests/ のみ), Bash(テスト実行のみ)
出力形式: 実装前提のテストコード一式
規約: 各テストに、検証している仕様ID(F-001等)をコメントで付与

#### implementer.md
役割: 仕様とテストを満たす実装を書く
できること: 実装コードの読み書き、テスト実行
できないこと: テストコードの変更、仕様書の変更
ツール権限: Read, Write(./tests/ 以外), Bash
規約: 仕様書とテストを満たすことを最優先。
      テストを通すためにテストを変更してはならない。
      テストが仕様と矛盾していると判断した場合は、
      実装を止めてオーケストレーターに報告する。

#### auditor.md
役割: 仕様書とコードを突き合わせ、各要件の実現状況を報告
できること: 仕様書・コード・テストの読み込み、レポート作成
できないこと: コード・テスト・仕様の変更
ツール権限: Read, Write(./reports/ のみ)
出力形式: ./reports/audit-YYYYMMDD-HHMM.md
レポート要件:
- 各機能ID(F-001等)について、実現箇所をファイル名と行番号で引用
- 引用できない要件は「未実装」と明記
- 仕様にない実装(オーバー実装)も指摘
- テストカバレッジと、テストされていない振る舞いを列挙

### 4. WORKFLOW.md(プロジェクトルート)
以下の内容で作成。

# 実装ワークフロー

実装は機能単位で完結させる。F-001を完全に終わらせてから
F-002に進む。並行禁止。

各機能について、以下の8ステップを順に実行する。
各ステップで、オーケストレーター(メインセッション)は
人間ユーザーの承認を得るまで次に進まない。

## ステップ1: 仕様確認
SPEC.md から該当機能の節を切り出し、未決定事項に該当機能が依存する
項目があれば、まず人間に確認する。曖昧さが残ったまま次に進まない。

## ステップ2: テスト設計
test-writer サブエージェントに、TEST_STRATEGY.md と SPEC.md の該当
機能を渡し、テストコードを生成させる。実装ファイルは作らない・見ない。

## ステップ3: テストレビュー(人間)
人間がテストコードを読み、受け入れ条件と対応しているか、
境界条件が網羅されているかを確認。承認するまで次に進まない。

## ステップ4: テスト実行(失敗確認)
実装がない状態でテストを走らせ、全テストが「失敗する」ことを確認。
「テストが何もしなくても通ってしまう」バグを防ぐため。

## ステップ5: 実装
implementer サブエージェントに、SPEC.md と該当テストを渡し、
テストを通す実装を書かせる。テストコードは変更禁止。
仕様にない機能の追加も禁止。

## ステップ6: テスト実行(成功確認)
全テストが通ることを確認。通らなければ implementer に戻すが、
3回失敗したら一度立ち止まり、人間に状況を報告する(無限ループ防止)。

## ステップ7: 監査
auditor サブエージェントに、SPEC.md の該当機能と実装コードを
突き合わせさせ、受け入れ条件ごとに実装箇所を引用しながら
レポートを作成させる。レポートは ./reports/ に保存。

## ステップ8: 完了承認(人間)
人間が監査レポートを確認し、次の機能に進む承認を出す。
この機能の実装中に発見された新たな未決定事項や設計判断を、
./decisions/ にADRとして記録する。

## ステップ間で守ること
- ステップを飛ばさない。「自明だから」も禁止
- 一つのステップ内で複数機能を扱わない
- テストの失敗確認(ステップ4)を省略しない
- 監査(ステップ7)で問題が見つかったら、ステップ5に戻る
- 監査レポートは消さず、./reports/ に時系列で蓄積する

## 機能をまたぐ判断
- ある機能の実装中に、別機能の仕様変更が必要と判明したら、
  実装を止めて人間に報告する。勝手に他機能の仕様を変えない
- 共通基盤(認証、ロギング等)が必要と判明したら、
  独立した「F-基盤-XXX」として切り出して先に実装する

### 5. 動作確認
構築後、以下を実行して報告:
- ディレクトリ構造のtree出力
- 各サブエージェント定義ファイルの先頭10行
- CLAUDE.md と WORKFLOW.md の全文

## 重要な制約
- この環境構築のセッション内で、SPEC.mdの実装には一切着手しない
- サブエージェントを「使う」のは次のセッション。今は「定義する」だけ
- 各サブエージェント定義は、そのエージェントが単独で動いたときに
  役割を逸脱しないよう、明確な禁止事項を含めること
```

### フェーズ2終了時のプロジェクト構造

```
project-root/
├── .claude/
│   ├── agents/
│   │   ├── spec-reviewer.md
│   │   ├── test-writer.md
│   │   ├── implementer.md
│   │   └── auditor.md
│   └── hooks/
│       └── log-event.ps1     (worklog自動記録ロガー)
├── specs/
│   ├── SPEC.md
│   ├── TEST_STRATEGY.md
│   ├── ARCHITECTURE.md       (Mermaidのアーキテクチャ図=進捗マップの素地)
│   └── status.json           (進捗の真実の源。dashboard.htmlの元データ)
├── decisions/        (空、これからADRを蓄積)
├── reports/          (監査レポート・worklog.jsonl・エフォートレポート)
├── tests/            (空、test-writerが書く)
├── dashboard.html    (生成物。進捗ダッシュボード)
├── CLAUDE.md
└── WORKFLOW.md
```

---

## フェーズ3: 実装ループ

Claude Code の新規セッションを開き、以下を実行する。

### プロンプト3

```
これから ./specs/SPEC.md の実装を、./CLAUDE.md と ./WORKFLOW.md の
ルールに従って進める。あなたはオーケストレーターとして振る舞い、
自分でコードを書かない。

## まず最初にやること
1. spec-reviewer サブエージェントを呼び、SPEC.md の曖昧な点や
   矛盾を洗い出させる
2. spec-reviewer の報告を私に提示し、私の回答を待つ
3. 私の回答を反映して SPEC.md を更新(または補足ファイルを作成)
4. 機能を独立して検証可能な単位に分割した実装計画を提案し、
   私の承認を得る
5. 承認後、機能ごとに WORKFLOW.md のステップ1〜8を回す

## 守ること
- 私の承認なしに次のステップに進まない
- 曖昧な判断が必要な場面では必ず私に質問する
- サブエージェントを呼ぶ際は、どのエージェントに何を依頼するか
  事前に私に伝える
- 複数機能をまとめて進めない。一機能ずつ完了させる

まず、SPEC.md を読み込んで、spec-reviewer の呼び出しから
始めてください。
```

---

## 人間が握る4つのポイント(再掲)

このフロー全体を通して、Wataruさんが意思決定者として握る場所:

1. **SPEC.md と TEST_STRATEGY.md のレビュー**
   - フェーズ1の段階2・3・5の出力をレビュー
   - 「未決定事項」セクションの内容を吟味し、放置できないものは
     ChatGPT/Claudeに戻って詰める

2. **機能分割の判断**
   - フェーズ3のステップ4(計画提案時)
   - AIが提案した分割を鵜呑みにせず、自分の検証能力に合った
     粒度にチューニング

3. **各ステップの承認**
   - WORKFLOW.md のステップ3(テストレビュー)
   - WORKFLOW.md のステップ8(完了承認)
   - 「説明できないものは承認しない」を規律にする

4. **ADRへの記録**
   - 各機能の完了時に、その実装で得られた設計判断を
     ./decisions/ に蓄積
   - 後で「なぜこうなっているか」を説明する材料

---

## トラブルシューティング

### Q. サブエージェントが役割を逸脱する
A. .claude/agents/ の該当ファイルの「できないこと」セクションを強化する。
   特に「○○の場合は実装を止めて報告する」という終了条件を明示。

### Q. テストが仕様と矛盾していると implementer が報告してきた
A. これは正常な動作。テストの方が正しいか、実装側の解釈が正しいかを
   人間が判断する。多くの場合、SPEC.md の該当箇所が曖昧だったので、
   ここで仕様を明確化して SPEC.md を更新する。

### Q. 監査レポートで「未実装」が出た
A. ステップ5に戻る。implementer に監査レポートを渡して再実装させる。
   3回繰り返しても解決しなければ、SPEC.md の該当要件自体に問題が
   ないか人間が再検討する。

### Q. 一機能が大きすぎて検証しきれない
A. 機能分割が粗かった証拠。その機能を F-XXX-a, F-XXX-b に分割し直す。
   「Wataruさんが一回のレビューで判断しきれるサイズ」が最適粒度。

### Q. 想定外の作業が発生した(例: 共通基盤が必要と判明)
A. その作業を新しい F-基盤-XXX として SPEC.md に追加し、
   依存関係を整理してから優先実装する。元の機能は一時停止する。

---

## このフローの設計思想

このフローは、3つの問題に対する構造的な防御線として設計されている。

### 防御線1: 仕様の曖昧さによる解釈ミス
- フェーズ1の段階出力と自己レビュー
- spec-reviewer サブエージェントによる事前洗い出し
- 「未決定事項」セクションの明示的な存在

### 防御線2: AIの自己採点バイアス
- test-writer と implementer の役割分離
- 監査セッションの独立性(auditor)
- ツール権限の物理的な分離

### 防御線3: 人間の検証能力の限界
- 機能単位での小さな完結
- 各ステップでの承認ポイント
- ADRと監査レポートによる意思決定の痕跡化

このフローを使い続けることで、Wataruさんは「AIに射程を超えさせつつ、
自分の意思決定として組織に説明できる」状態を保てる。
