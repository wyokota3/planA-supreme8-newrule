# 実装ワークフロー

実装は機能単位で完結させる。F-001を完全に終わらせてからF-002に進む。並行禁止。

各機能について、以下の8ステップを順に実行する。
各ステップで、オーケストレーター(メインセッション)はユーザーの承認を得るまで次に進まない。

## ステップ1: 仕様確認

- `./specs/SPEC.md` から該当機能の節を切り出す
- 未決定事項に該当機能が依存する項目があれば、まずユーザーに確認
- 曖昧さが残ったまま次に進まない

## ステップ2: テスト設計

- `test-writer` サブエージェントを呼ぶ
- 渡すもの: `./specs/TEST_STRATEGY.md` と該当機能の仕様
- 出力: `./tests/` 配下のテストコード
- **制約**: test-writer は実装コードを見ない・書かない

## ステップ3: テストレビュー(人間)

- ユーザーがテストコードを読み、受け入れ条件と対応しているか確認
- 境界条件が網羅されているか確認
- ユーザーの承認を得るまで次に進まない

## ステップ4: テスト実行(失敗確認)

- 実装がない状態でテストを走らせる
- 全テストが**失敗する**ことを確認
- 「テストが何もしなくても通ってしまう」バグを防ぐため

## ステップ5: 実装

- `implementer` サブエージェントを呼ぶ
- 渡すもの: 該当機能の仕様とテストコード
- **制約**: テストコードは変更禁止。仕様にない機能の追加も禁止
- テストが仕様と矛盾していると判断した場合は実装を止めてオーケストレーターに報告

## ステップ6: テスト実行(成功確認)

- 全テストが通ることを確認
- 通らなければ implementer に戻す
- **2回失敗が続いたら、3回目の前にモデル昇格を提案**:Pro/Max5 で implementer が安いモデルの場合、`/set-plan` で一段上げる(または `implementer.md` の `model:` を手動で上げる)とコスト効率よく突破できることがある。安いモデルで失敗を繰り返す方がかえって制限を消費するため
- **3回失敗したら一度立ち止まり、ユーザーに状況を報告**(無限ループ防止)

## ステップ7: 監査

- `auditor` サブエージェントを呼ぶ
- 渡すもの: 該当機能の仕様と実装コード
- 出力: `./reports/audit-YYYYMMDD-HHMM-F-XXX.md`
- レポート要件: 各受け入れ条件について実装箇所をファイル名と行番号で引用
- 監査モデルの差し替え(任意): `HARNESS_AUDIT_PROVIDER`(既定 `existing`)で provider を切り替えられる。Copilot CLI が使える人は `copilot` を選べる。既定では従来どおりサブエージェントが監査する。詳細は `docs/AUDIT_PROVIDERS.md` / `.harness/audit/README.md` 参照

## ステップ8: 完了承認(人間)

- `specs/status.json` を更新する:該当機能と対応ノードの `status` を `done` に、`files`(実装箇所)・`audit`(監査レポートパス)・`completedAt` を埋める
- `/dashboard` を実行して `dashboard.html` を再生成する
- ユーザーに承認を求めるとき、以下を必ず提示する(「何がどこまでできたか」を直感的に示すため):
  - **今作ったのは図のココ**:完了したノードを名指しし、`dashboard.html` で緑化したことを伝える
  - **進捗マップの before/after**:全体進捗%がいくつからいくつに上がったか
  - **データフローの状況**:end-to-end で通った経路と、まだスタブの経路
- ユーザーが監査レポートと進捗マップを確認し、次の機能に進む承認を出す
- この機能の実装中に発見された新たな未決定事項や設計判断を `./decisions/` にADRとして記録

## ステップ間で守ること

- ステップを飛ばさない。「自明だから」も禁止
- 一つのステップ内で複数機能を扱わない
- テストの失敗確認(ステップ4)を省略しない
- 監査(ステップ7)で問題が見つかったら、ステップ5に戻る
- 監査レポートは消さず、`./reports/` に時系列で蓄積する

## 機能をまたぐ判断

- ある機能の実装中に別機能の仕様変更が必要と判明したら、実装を止めてユーザーに報告
- 勝手に他機能の仕様を変えない
- 共通基盤(認証、ロギング等)が必要と判明したら、独立した `F-基盤-XXX` として切り出して先に実装する

## コミット規約

各ステップ完了時にgit commitを打つ。メッセージ形式:

```
[F-XXX] step<N>: <ステップ名>

- 詳細1
- 詳細2
```

これにより、問題があったときに任意のステップに戻せる。
